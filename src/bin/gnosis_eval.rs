//! §7.8 F6 — the `gnosis-eval` `[[bin]]`.
//!
//! Contract: `docs/specs/6-f6-eval-spec.md` §7. The real bin reads a labeled
//! corpus JSON (§8 schema), seeds a `Store`, runs `rag_query` per case, computes
//! the four metrics, and prints a per-case + aggregate report.
//!
//! **RED-STAGE STUB.** This is a placeholder the Implementer replaces. The
//! TestWriter does NOT implement the bin's real logic here.

fn main() {
    todo!("F6 gnosis-eval bin not implemented (RED stub)")
}
