# Gnosis — Active Defect / Requirement-Gap List

Maintained by the document-archival loop. Open gaps on top; fixed rows below.
A defect in a foundation/package (or a gap owned by another suite tool) is a
HANDOFF item — never patched here. A host-side finding (this project's `src/`)
is fixed here.

## OPEN

**From U2's adversarial pass (2026-09-16).** The rows `P-1`/`P-2`/`P-3`/`P-4` below were filed by the
U2 (query POST contract) adversarial gate. **From the U2 proofread pass (2026-09-17):** `P-5` (the SSE
error-frame media-type divergence found by the live battery's F-2 finding) and `P-6` (the reported
wrongly-typed-`n` rule-vs-code divergence) were added by the documentation audit — neither is an adversarial-gate
finding, and **neither is a U2 regression**. **Supervisor dispositions (2026-09-16) on the two proofread
rows:** **`P-6` is FIXED/CLOSED** — the pinned clause (a present wrongly-typed **documented member**,
`n` included, ⇒ the whole option is `None`) was ruled **correct and NOT narrowed**, and the decoder is
implemented to it instead; and the proofreader's **lower-severity companion** (the numeric cells readable as
claiming a 400 for a non-`u64` number) is filed as **`P-7`** and **FIXED/CLOSED docs-side** (wording
corrected, no behavior change). Both rows keep their historical evidence and stand below with their
resolutions. **U2 stayed scope-clean:** every one of the adversarial-gate four is
**host-external or foundation- or test-side** — three are **pre-existing** (they are *not* U2
regressions and U2 did **not** introduce them) and the fourth is a **latent trip-wire in the test
harness**, closed test-side by the TestWriter in the same pass. Per the adversarial gate rule
(*host findings are fixed in the same pass; package/foundation findings go to
`docs/defects.md`/`docs/HANDOFF.md`*), none of them was absorbed into U2 — each carries a named owner
unit and a revisit trigger. The three engine-side engine-owning items are cross-filed as **this
repo's own deferred work** in `docs/HANDOFF.md` §"U2 adversarial pass (2026-09-16) — engine-side
package items" (an engine-owned block, explicitly **not** an upstream/canonical-contract ask). The
U2 DONE row itself is written by the supervisor after
the whole gate chain finishes — **DONE (see `docs/next-steps.md` §DONE's U2 row; the post-greens
documentation review reconciled these rows against the landed tree in the same pass —
`archive/reviews/2026-09-16-u2-query-post-contract-doc-review.md`)**.

**From the U3 documentation pass (2026-09-17, proofreader).** The rows `P-8` and `P-9` below were filed by the
**U3 (status honesty) documentation audit**, in the same pass that marked the hard-coded-flags row **FIXED (U3)**.
**Neither is a U3 regression** — every pinned U3 row passes (five typed rows HELD, blind set 13/13, live battery
9/9) — and neither is an upstream/canonical-contract ask: both are this repo's own deferred transport/codec
work, cross-filed in `docs/HANDOFF.md` §"U3 documentation pass (2026-09-17) — engine-side deferred items".
**All nine rows below were re-verified against the landed tree by the U3 post-greens documentation review
(gate 8, 2026-09-17):** `P-1`/`P-2`/`P-3`/`P-5`/`P-8`/`P-9` **OPEN**, the hard-coded-flags row **FIXED (U3)**,
the `Ready`-axis row **OPEN (document-only)**, `P-4`/`P-6`/`P-7` **FIXED/CLOSED**; the drifted citations in
`P-5` and the two U3-adjacent rows were re-pointed in that same pass (record:
`archive/reviews/2026-09-17-u3-status-honesty-doc-review.md`).

**From U5's adversarial pass (2026-09-22, gate 4), re-verified by the U5 documentation review (gate 8, same day).**
The rows **`U5-ADV-1`**…**`U5-ADV-5`** below were filed by the U5 (boot vector-index build) adversarial gate, which
found **no U5 regression requiring a host code fix in that pass**; the sixth U5-pass row is **`P-9`'s TRIGGER
FIRED**. **All six stay OPEN** — a change that fixes a defect must land through its own spec + red set, so none was
absorbed. `U5-ADV-1` (the boot index is frozen ⇒ a post-boot write makes `mode=vector` a **silent 200 with
missing hits** while `vector` still reports `true`; **live-confirmed by gate 6**, which found the seeded document
absent from the vector index) is the primary escalation and a follow-up **"U5 honesty/freshness"** unit is being
opened for it. `U5-ADV-2`…`U5-ADV-5` are the residual-risk, diagnostics and lock-class rows.
**Gate-8 verification of the U5 rows against the landed tree (2026-09-22, read-only):** every `file:line` above
was re-read; the drifted citations in `U5-ADV-1` (the build call site added), `U5-ADV-3` (`:4226-4230` ⇒
`:4229-4233`, the string at `:4230`), `U5-ADV-4` (`built.ok().flatten()` is at `src/bin/gnosis_server.rs:416-419`)
and `U5-ADV-5` (U5's own lock site is `src/store/mod.rs:5386`; the six-line list this row carried was mostly
stale) were corrected in place with dated markers, and the U5 code facts were confirmed: the seam
`build_boot_vector_index` at `src/store/mod.rs:5369` (body `:5369-5414`), the boot wiring at
`src/bin/gnosis_server.rs:381-437` (build `:414`, composed snapshot `:416-419`, order `:425-432`, wiring
`:433-437`), the derivation at `src/store/mod.rs:4204-4224` and the fixed `lastError` string at `:4230`.
**The PBT audit's `T1`–`T10` negative-generator list is NOT a row here — it is an undischarged TestWriter
obligation**, recorded in `docs/next-steps.md` (the U5 CURRENT WORK block + the U5 DONE row).

| Defect / gap | Where it stands | Note |
| --- | --- | --- |
| **P-1 — unbounded request-body buffering on every route (pre-existing, not a U2 regression)** | `src/bin/gnosis_server.rs:191` (`crud_handler`) and `src/bin/gnosis_server.rs:206` (`rag_query_handler`) — both take `body: String`; no `DefaultBodyLimit`/length cap is configured anywhere in the bin (a tree-wide search for `DefaultBodyLimit`/`BodyLimit` yields **no hits**). Whole-tree materialization: `src/wire/envelope.rs:41-58` (`Envelope::from_json`) parses the **entire** JSON tree into a `serde_json::Value` before any decode/validation decision, and `serde_json/preserve_order` is enabled (`Cargo.toml:11`), so the retained tree is order-preserving and larger than a plain map. | **OPEN (2026-09-16, U2's adversarial pass).** A process that can reach the loopback port can drive **O(body) heap** before any decode decision is taken — the only bound today is the OS/process limit (no per-route or per-server cap, no over-cap fail-state). **Pre-existing: NOT introduced by U2** (both handlers predate U2 and U2 changed only the decode path behind them; U2 added no new unbounded read). This is currently a **documented-but-bounded** risk: the gate-1 residual-risk register already accepts "any process that can reach the loopback port" (`docs/specs/gnosis-gr-inbound-review.md` §Residual risk register, the loopback-only + shell-owned-RBAC row) — but note that register's *separate* "Unbounded payloads on the new list routes" row mitigates **U4's** list routes only; it does **not** cover this transport-level body cap, so this row is the home for the transport gap. **Disposition: OPEN — owner = a later transport unit** (a bin-scoped `DefaultBodyLimit` plus the over-cap fail-state need their own spec + register rows; a request-body cap is a wire-visible 413-class outcome and must not be invented inside a decode-path unit). **Trigger to revisit:** any non-loopback bind, any engine-owned durable/large-payload surface (GR-6 bulk ingestion, GR-7 persistence), or the transport unit's own scheduling — whichever comes first; the fix is a spec'd cap + fail-state, not an ad-hoc `DefaultBodyLimit` call. |
| **P-2 — the SSE route's `Query<HashMap<String, String>>` extractor rejections bypass the shared decode-error renderer (pre-existing)** | `src/bin/gnosis_server.rs:247` (`rag_stream_handler`'s `Query(params): Query<HashMap<String, String>>`). | **OPEN (2026-09-16, U2's adversarial pass).** A malformed query string (e.g. `?mode=a&mode=b`, or any pair that fails to deserialize into `HashMap<String, String>`) is rejected by **axum's own extractor** and rendered as axum's **plain-text** 400 **before the handler body ever runs** — so §5.5's "one shared rendering … every route that can fail to decode a request" (`docs/specs/p2-gnosis-server.md` §5.5 U1-addition "One shared rendering", which names the CRUD handler and the query handler, `src/bin/gnosis_server.rs:143-153`, `:157-161`) has an **unlisted hole** on the SSE route: the SSE surface has a decode-failure path that never reaches `decode_error_response`. **Pre-existing: NOT introduced by U2** (the `Query` extractor predates U2; U2 changed only what happens *after* the handler is entered). **Unreachable from the pinned cases** — no register conformance/property row exercises a malformed SSE query string, so nothing is red today; this is a contract-coverage hole, not a live failing behavior. **Disposition: OPEN — owner = a later transport unit** (removing the hole changes the SSE route's contract: the route must take the raw query string and render through `decode_error_response` itself, or a rejection hook must be registered — either is a contract amendment with its own spec + register rows, hence out of U2's scope). **Trigger to revisit:** the transport unit that owns §5.5's rendering, any consumer report of a plain-text 400 from `GET /rag/stream`, or any U4 change to the SSE/change-feed routes that re-touches this extractor. |
| **P-3 — `validate_rag_result` does not check mode/trace agreement or citation integrity (foundation scope — F2's validator)** | `src/wire/decode.rs:63-71` (`validate_rag_result`). | **OPEN (2026-09-16, U2's adversarial pass).** The validator checks exactly two things — `engine != "gnosis"` → `WrongEngine`, and `blocked_by.is_some()` with a non-`RagTrace::Graph` trace → `BlockedByWithoutGraphTrace` — and returns `Ok(())` otherwise. So a `RagTrace::Vector(TraceDescriptor { mode: Flat, … })` (a trace whose recorded mode disagrees with the trace variant) or a result whose citations **contradict** its `results` **validates successfully**. **Consequently the register row `P-TP-4`'s prose over-claims:** it describes the well-formed corpus as "`engine == \"gnosis\"`, trace present **and mode-consistent**, `blocked_by` only with a `RagTrace::Graph`" (`docs/specs/p2-gnosis-server.md` §9.5.1 `P-TP-4`), but the validator enforces **no** mode-consistency clause and **no** citation-integrity clause. **Prose-vs-validator gap, recorded as such** (the prose is aspirational relative to today's validator; U2's `P-TP-4` row is honest about the *encoder/validator* totality it pins — `encode_result_checked` → `decode_rag_result` → `validate_rag_result` — so its green does **not** certify the unenforced clauses). **Foundation scope: NOT a U2 regression** — the validator is F2 wire-foundation code, and U2 consumes it (the checked encoder) rather than owning it. **Disposition: OPEN — owner = the F2 wire foundation** (strengthening the validator is its own unit with its own register rows: it adds two new rejection classes, i.e. new `ValidationFailure` variants and new §11-fail-state/rendering decisions, which must not be smuggled into a server-contract unit). **Trigger to revisit:** the F2 validator strengthening unit being scheduled, any consumer/client that relies on mode-consistency or citation integrity of an engine-emitted result, or the next unit that touches `src/wire/decode.rs`'s validator. |
| **P-4 — latent panic trip-wire in the property harness (test-side)** | `tests/props_gnosis_server.rs:1559-1586` (`casing_sample`; the loop statement at `:1575-1584` — the fn was `:1515-1542` when this row was written, and the pre-U2 citation `:1296` is stale): `let idx = 1 + rng.below((all.len() - 1) as u64) as usize;`). | **FIXED (2026-09-16, U2 adversarial-fix pass — test-side).** The harness samples the non-canonical casings by index: `1 + below(all.len() - 1)`. For a **1-character** token `all.len() - 1 == 0` ⇒ `below(0)` (an RNG misuse/panic), and for an **empty** token the `all.len() - 1` subtraction would **underflow** before `below` is even reached. **Unreachable with today's corpora** — `sample_k` is `1` for tokens of ≤3 chars (`tests/props_gnosis_server.rs:1589-1595` — the fn was `:1545-1551` when this row was written, and the pre-U2 citation `:1307-1309` is stale), so `n = min(all.len(), k) = 1`, the `while out.len() < n` loop body never runs, and `below` is never called — **but it is a trip-wire for any 1-char or empty token added to a future vocabulary** (the two call sites are the `U2PIM6` and `U2PSM4` rows: `:2929` and `:3160`/`:3174`/`:3186` — the citations `:2863`, `:3094`/`:3108`/`:3120` this row carried before the post-greens doc-review were stale). **Owner = the TestWriter, in the same U2 adversarial-fix pass**, which lands the guard `if all.len() <= 1 { return out; }` (**landed at `:1567-1569`**; the early return also removes the underflow path — the row's earlier `:1523-1525` citation was the same drift). **Recorded here as FIXED** rather than as an open row: it is test-side, the fix is a guard the TestWriter owns, and this docs pass **did not edit the test file**. **Revisit trigger:** none owed — the guard is the fix; if the TestWriter's guard does **not** land in the U2 adversarial-fix pass, reopen this row as an OPEN test-side defect (owner: TestWriter) with the same `file:line` evidence. |
| **P-5 — `GET /rag/stream`'s pre-stream `StoreError` `error` frame is rendered `text/plain; charset=utf-8`, contradicting the newly pinned SSE media type (pre-existing, not a U2 regression)** | `src/bin/gnosis_server.rs:272-281` (the `store.rag_stream` `Err(e)` branch returns `(StatusCode, String)` — the frame bytes go through axum's `String` responder ⇒ `text/plain; charset=utf-8`; **the `:264-281` citation this row carried is stale** — the U3 landing moved the branch to `:272-281`), versus the U2 pre-stream renderer at `:100-115`, which sets `(header::CONTENT_TYPE, "text/event-stream")` (**the `:99-114` citation this row carried is stale for the same reason: the U3 landing shifted the fn to `:100-115`**). | **OPEN (2026-09-17, the U2 proofread pass — the live battery's F-2 finding).** `docs/specs/p2-gnosis-server.md` §5.4's new F-2 bullet (with §5.6, §10 and `docs/specs/engine-wire-contract.md` §4.4) now **pins `Content-Type: text/event-stream` for every SSE response** — the pre-stream `error`-frame responses included (the `mode` 400 *and* the §11-mapped pre-stream frames, e.g. the not-READY **503**). The landed state is **partially divergent**: the U2 path emits the pinned media type, while the **pre-existing** post-stream `StoreError` branch emits the same frame bytes as `text/plain; charset=utf-8`. **Pre-existing: NOT introduced by U2** (that branch predates U2; U2 only re-pointed the `mode` failure to the new renderer). **No in-repo test asserts the SSE header**, so nothing is red today. **Disposition: OPEN — owner = the transport unit / the SSE branch** (setting the header is a transport-rendering change; the same unit should verify the **`200` frame stream's** own header against the pin, which also has no test). **Trigger to revisit:** the transport unit that owns the SSE rendering, any consumer report of a `text/plain` SSE response, or any change to the SSE route's response construction. **Cross-filed** in `docs/HANDOFF.md` §"U2 adversarial pass (2026-09-16) — engine-side package items" (this repo's own deferred work; explicitly **not** an upstream ask). |
| **P-6 — the pinned wrongly-typed-`n` member clause was not implemented by the landed U2 decoder (host-side, rule-vs-code divergence)** | `src/wire/query.rs` — the pre-fix `enabled_object` inspected only the `enabled` member (`:183-190`) plus `n: mq.get("n").and_then(Value::as_u64).unwrap_or(3)` (`:129-134` ⇒ a wrongly-typed `n` defaulted inside a **present** object) — **the post-fix (landed) total member check is `object_option`, `src/wire/query.rs:188-202` (`enabled` ⇒ bool, `n` ⇒ u64); the pre-fix line numbers above are kept as the finding's evidence**; the clause it diverged from: `docs/specs/p2-gnosis-server.md` §5.3's `multiQuery` cell + two-layer bullet, §9.5.1 `P-IM-5`, §9.5.4's `P-IM-5` note, §10, §11's `P-IM-5` sketch, and `docs/specs/engine-wire-contract.md` §4.5/§13. | **FIXED / CLOSED (2026-09-16 — the supervisor ruled the pinned clause CORRECT and did NOT narrow it; the decoder is being implemented to it instead).** The pinned clause is explicit: *"an object whose documented member (`enabled`, or `n`) is present with the wrong type ⇒ `multi_query == None`"*, naming the instance `{"multiQuery":{"enabled":true,"n":"3"}}` ⇒ **field-level `None`**. The landed behavior for that input was `Some(MultiQueryOptions{ enabled:true, n:3 })`, and the executed property layer asserted **exactly that landed reading** (`tests/props_gnosis_server.rs:1187-1193` — `object_option_is_well_formed` consulted `enabled` only; `:1335-1350` — `absorbed_expectation` defaulted `n`; corpus `:1177`; row `U2PIM5` at `:2431`), so **no test caught the divergence** (the blind set's `U2-13`/`U2-13b` covered the `enabled` misuse only). **Resolution:** the decoder is **generalized to every documented member** — `enabled` a bool and `n` a u64 for `multiQuery`; `enabled` a bool for `subTaskDag` (`object_option` and its member-type arms — `member.is_boolean()` for `enabled`, `member.as_u64()?` for `n` — `src/wire/query.rs:178-202`, total over the declared members of both object-valued options) — so the pinned input now decodes to `multi_query == None`, while an **omitted** `n` inside a **well-formed** object still defaults to `3`. The code change and the TestWriter's aligned probe (`object_option_is_well_formed`/`absorbed_expectation` must check `n` too, and the row's doc comment at `:1170-1172`, which cited §5.3 for the opposite reading, is corrected) land in the **same U2 pass**; the spec text was **not** weakened, and no register row was added or deleted. **Owner:** the Implementer (code) + the TestWriter (probe), one U2 pass. **Trigger to revisit:** if the aligned probe does not land in the same pass, reopen as an OPEN test-side row (the pinned clause is now the only permitted reading). |
| **P-7 — §5.3's numeric "out of range ⇒ 400" cells could be read as claiming a 400 for a non-`u64` JSON number (docs-side rule-vs-behavior divergence, wording only)** | `docs/specs/p2-gnosis-server.md` §5.3's per-key table (`topK`/`maxHops`/`binaryCandidatePool` cells: *"a number out of `1..=50` ⇒ FS-3 400"*) + its fail-state table's range row + its wrongly-typed bullet, §10's cross-cutting list, and the same claim in `docs/specs/engine-wire-contract.md` §4.5/§13/§16 — versus the landed decoder's `Value::as_u64` reads (`src/wire/query.rs:119`, `:126`, `:128`, `:140`). | **FIXED / CLOSED (2026-09-16 — supervisor ruling 1: the WORDING was corrected, no behavior change).** `as_u64` cannot represent a **negative or fractional** JSON number, so `topK:-1`/`topK:10.5` is **absent ⇒ the option's documented default**, **not** a 400; only a **present `u64`** outside the pinned range (`topK:0`/`51`, `maxHops:0`/`6`, `binaryCandidatePool:0` with `binaryFirstPass:true`, `multiQuery:{enabled:true,n:0}`) reaches the engine's `ValidationError` (400 `validation_error`). The cells now state the **two halves** distinctly (`p2` §5.3's new **type-vs-range** bullet is the authority, cross-referenced from §5.3's two tables, §10, §9.5.1's note, and F2 §4.5/§13/§16) and **cannot** be read as a 400 for `-1`/`10.5`; the historical wording is retained in `p2`'s §U1 status block as the record. **No test pinned that input either way** (the corpus used strings and `u64` boundaries), so nothing went red and **no code was touched** — this was a docs-side defect only. **Owner:** the SpecWriter (closed by the 2026-09-16 docs pass). **Trigger to revisit:** none owed; if a future unit decides a non-`u64` numeric value *should* be a 400, that is a **rule change** requiring its own spec-amendment unit and register row (it would also make `-1` and `"10"` diverge, which today's wrongly-typed rule keeps together). |
| **P1a/P2 landing was not `fmt`-clean or `clippy`-clean (docs claimed "clippy 0 warnings · fmt clean")** | `src/bin/gnosis_server.rs`, `src/wire/crud.rs`, `tests/{crud_wire_conformance,gnosis_server_conformance,gnosis_server_e2e,props_crud_wire,props_gnosis_server,blind_p1a_crud_wire_greens,blind_p2_gnosis_server_greens}.rs` | **FIXED (2026-09-10, host-side hygiene pass).** `cargo fmt --check` exited 1 (48 diff sites in the P1a/P2 files) and `cargo clippy --all-targets` emitted ~50 warnings (41× `clone` on `Copy` `CrudMethod`, 4× `clippy::doc_lazy_continuation`, 3 dead reference-enumerator fns, 2 `clippy::zombie_processes`). All resolved: `cargo fmt`, `clippy --fix --all-targets`, plus justified `#[allow]`/doc-para fixes (dead fns are spec-citation reference enumerators; the spawned server child is correctly reaped via `ServerGuard`'s `kill`+`wait`). Trio re-verified: **`fmt --check` exit 0 · `clippy --all-targets` 0 warnings · `build` clean · `cargo test` 538 pass / 0 fail.** Pure formatting + lint hygiene, no behavioral change. |
| **§8 RBAC `caller` threading is unsatisfiable against the `RagStore` surface** | `src/store/mod.rs` (the mutating `RagStore` methods take no `caller` param) | **CLOSED as re-scoped to the shell (2026-09-10, proposal gate — `docs/specs/rbac-caller-review.md`).** The engine-side RBAC enforcement is deliberately NOT implemented: the engine has no authority-mapping source (the only "who may edit" mapping is the shell's boot-time `AuthorityStore`), and the C5 boundary classification + the pure-backend framing place the authorization gate at the SHELL. The shell's A2 caller-side deny (`handleGnosisTool` `resolveCallerCredential` → `Error('<tool>: caller has no edit authority')`, fail-closed, before any proxy call) **is** the enforcement. The P2 server threads the `caller` through the request-decode layer only (validates presence on the 7 mutating methods → 400 if missing, per P1a §10; does NOT pass it to the engine). Decision `GNOSIS-RBAC-EDIT-ENFORCEMENT` amended to align with C5 (RBAC → SHELL). Residual risk (accepted): the engine's mutating methods are callable by any process that can reach the loopback port directly; acceptable under the loopback-only + local-shell-only trust model. Trigger condition for revisiting: a future non-shell direct client → mitigation is a caller-aware decorator at the access boundary (Option B), NOT a trait change. |
| **`subTaskDag` is a validated-only no-op** | `src/store/mod.rs` (option parsed + validated; nothing decomposed) | **DOCUMENTED, NOT IMPLEMENTED** (decision SUB-TASK-DAG-VALIDATED-ONLY). The inference-time sub-task DAG is opto-in / SHOULD-HAVE (like adaptive RAG, which is PARKED). A single-node decomposition (the query itself) is the honest stand-in; **no fake decomposition** is fabricated. `SubTaskDagFailed` is a **RESERVED** variant. Parked in `docs/pending.md`. |
| **`CompressionFailed` is reserved (no fail-state)** | `src/store/mod.rs` (compressor is total) | The spec says the compressor **degrades gracefully to uncompressed** on failure; the local compressor (Filter/Extract/Graph) only **transforms** and never fails-and-degrades. `CompressionFailed` is a **RESERVED** variant for a future non-degradable compressor leg. The graceful-degrade behavior is **spec-correct**; this is **not** a missing fail-state (decision RESERVED-ERRVARIANTS-DISCIPLINE). |
| **`HyDEGenerationFailed` reserved; provider errors surface as `EmbeddingUnavailable` (LOW-E)** | `src/store/mod.rs` `vector_query` | `generate_hypothetical` is **total** (a deterministic stand-in), so it never constructs `HyDEGenerationFailed`. The prior code **relabelled** any `embed` provider error as `HyDEGenerationFailed`; fixed (LOW-E) so an embedding-provider failure surfaces as `EmbeddingUnavailable`. `HyDEGenerationFailed` remains **reserved** for the actual hypothetical-**generation** step when it becomes fallible. |
| **§4.4 `publish_document` concurrent-atomicity non-goal (TOCTOU)** | `src/store/mod.rs` `publish_document` | Publish validates target-liveness **under READ locks** but does **not** re-run the liveness/stale check under the shard write for the `DRAFT→PUBLISHED` transition, so a concurrent target mutation between validation and the transition can yield a publish with a later-**stale** reference. This is a documented, deliberately-***excluded*** non-goal of the §4.4 PBT register's `P-TP-2` (decision note), **NOT** asserted as an invariant. **Disposition:** engine-internal deferred — revisit at the F2/IPC seam pass if publish is used under contended writes. |
| **`/engine/status` asserts capabilities the engine does not have — the six `EngineSubsystems` flags are hard-coded `true`** | `src/store/mod.rs:1718-1725` (`EngineSubsystems { store: true, graph: true, lexical: true, vector: true, embedding: true, reranker: true }` — the LEGACY construction literal, **dead value after U3**, now sitting under its U3 comment block `:1714-1717`; **the `:1714-1725` citation this row carried is stale** — it mis-joined the comment block to the literal — and the pre-U3 citation was `:1710-1717`), versus the **landed derivation** `src/store/mod.rs:4193-4232` (`get_engine_status`'s read-time projection; the pre-U3 citation was `:4181-4195`) | **FIXED (U3, 2026-09-17).** Historical finding (kept as the record): all six flags were hard-coded `true` at construction, so `/engine/status` made **false status claims** — `reranker: true` although **no reranker implementation exists anywhere in `src/`**, and `vector: true` although the boot path swapped in `DerivedIndexes::default()` and left `vectors: None` (`src/bin/gnosis_server.rs:390-404`, landed; the pre-U3 citation was `:385` and the pre-U1 citation this row carries was `:329`) — i.e. the vector index was never built, so `mode=vector` could not work. Under the gate's ruling 4 a flag means "this subsystem's **full query-time capability** is wired and functional for the current store", which `lexical: true` satisfied honestly (live shard-scan BM25) but `reranker` and `vector` did not. **Disposition as written:** fix scheduled as unit **U3** (derive the six flags from real state; `reranker` false; `vector` false until **U5**'s boot index build), with the F2 §9 amendment + the **V-8 golden** update landing in the **same unit as the flag change** — **SUPERSEDED (2026-09-16): U2/U3 ARE AUTHORIZED — the registers are authored in `docs/specs/p2-gnosis-server.md` §9.5.1/§9.5.2, so the U3 code is authorized and merely owed; U4/U5 remain NOT authorized.** The historical clause read: "**U1's contract amendment is now LANDED (2026-09-16) — the F2 §9 amendment is in place; the code change is owed.**" **RESOLUTION (U3 LANDED-GREEN, 2026-09-17):** the six flags are now **derived at read time inside `get_engine_status`** (`src/store/mod.rs:4193-4232`) — `store`/`graph`/`lexical` true, `vector == snapshot().vectors.is_some()`, `embedding == embedding_provider().is_some()`, `reranker` false — **no flag mask is written anywhere in `src/`**, the boot applies only the returned `EngineState` plus its own wiring (the lib seam `boot_wiring`, `src/lib.rs:69-110`; the bin boot `src/bin/gnosis_server.rs:378-408`), `set_subsystems` is **observationally inert** (`src/store/mod.rs:1868-1876`), `EngineSubsystems` is still exactly six `bool`s and `HealthReport` gained **no** field. Verified: all five U3 rows HELD (`P-IM-7` 25 / `P-IM-8` 26 / `P-IM-9` 26 / `P-SM-5` 25 / `P-SM-6` 25 = **127 executed ≤ 400**), the V-8.1/V-8.2 literals amended in the same unit (`tests/wire_conformance.rs:1061-1112`), the live row `R-L2` PASS (2026-09-17). The historical text above stands as the record. | — `docs/specs/gnosis-gr-inbound-review.md`; go-ahead given for U0 only, 2026-09-16 — all units HELD; **SUPERSEDED (2026-09-16) as to U2/U3: U2 and U3 are AUTHORIZED and their typed registers are AUTHORED in `docs/specs/p2-gnosis-server.md` §9.5.1/§9.5.2 — only the U3 *code* is owed; U4/U5 remain NOT authorized). The historical clause read: "U1 is now authorized (2026-09-16) and U3 remains not-yet-authorized".**) All six flags are hard-coded `true` at construction, so `/engine/status` makes **false status claims**: `reranker: true` although **no reranker implementation exists anywhere in `src/`**, and `vector: true` although the boot path swaps in `DerivedIndexes::default()` and leaves `vectors: None` (`src/bin/gnosis_server.rs:385`, landed post-U2 — the pre-U1 citation this row carries was `:329`) — i.e. the vector index is never built, so `mode=vector` cannot work. Under the gate's ruling 4 a flag means "this subsystem's **full query-time capability** is wired and functional for the current store", which `lexical: true` satisfies honestly (live shard-scan BM25) but `reranker` and `vector` do not. **Disposition:** fix scheduled as unit **U3** (derive the six flags from real state; `reranker` false; `vector` false until **U5**'s boot index build), with the F2 §9 amendment + the **V-8 golden** update landing in the **same unit as the flag change** — **SUPERSEDED (2026-09-16): U2/U3 ARE AUTHORIZED — the registers are authored in `docs/specs/p2-gnosis-server.md` §9.5.1/§9.5.2, so the U3 code is authorized and merely owed; U4/U5 remain NOT authorized.** The historical clause read: "**U1's contract amendment is now LANDED (2026-09-16) — the F2 §9 + V-8 golden pins U3 must satisfy exist in `docs/specs/engine-wire-contract.md` §9.1/§12 — but U3 itself is not yet authorized** (the approved scope was U0+U1, re-gated at U1's spec review; the spec gate is now OPEN — see `docs/specs/gnosis-gr-inbound-review.md`)." |
| **P-8 — post-boot provider loss is invisible to `/engine/status` (the honest-signal gap the `P-IM-8` prose leaves open; pre-existing in kind, out of U3's scope)** | `src/store/mod.rs:4193-4232` (the read-time derivation: `embedding == self.embedding_provider().is_some()`, `state` = the stored `engine_state`) + `src/bin/gnosis_server.rs:378-408` (the boot sets `engine_state` **once**) — nothing re-evaluates `EngineState` (or the provider's reachability) after `main()`'s probe. | **OPEN (2026-09-17, the U3 live-scenario battery — a finding from the unit's own 9/9 live pass).** The battery proved that after a boot whose provider was **reachable and then became unreachable**, `GET /engine/status` still returns `state:"Ready"`, `embedding:true`, `lastError:null` — **byte-identical** to the healthy read (`tests/blind_u3_status_honesty_greens.rs::u3_12` pins that byte-identity as a *purity* claim; the live pass pins it as the *staleness* observation). So the "honest signal in that case is the `state`/`last_error` pair" that §9.5.2's `P-IM-8` prose promises is **never delivered for post-boot loss**: the pair is honest for the boot-time probe only. **This is NOT a U3 regression** — `P-IM-8` pins the flag as the *wired capability* (`embedding == provider.is_some()`), and the wired provider is still wired, so every pinned U3 row passes; the gap is in the **liveness of `state`/`last_error`**, which no U3 row claims. **Disposition: OPEN — owner = a later transport/status unit**, which must either (a) re-probe the provider on the status read (or on a bounded interval) and transition `Degraded` with the pinned `lastError`, or (b) pin an **explicit, documented staleness window** for `state`/`last_error` (the cheaper option: it makes the surface honest about being a boot-time snapshot). Either is a contract change with its own spec + register rows. **Trigger to revisit:** any consumer needing live provider reachability in the status surface (e.g. a shell health pane that must not show `Ready` for a dead provider), or the next unit touching boot/liveness. **Cross-filed** in `docs/HANDOFF.md` §"U3 documentation pass (2026-09-17) — engine-side deferred items" as this repo's own work (explicitly **not** an upstream ask). |
| **P-9 — `encode_result`'s `expect("RagResult is Serialize")` is a reachable panic path for a non-finite `score` (pre-existing; score-production side, out of U3's scope)** | `src/wire/codecs.rs:94-99` — `pub fn encode_result(res: &RagResult) -> Envelope { … serde_json::to_value(res).expect("RagResult is Serialize") }` (the `expect` is at `:98`); reached from the wire by `encode_result_checked` (`src/wire/query.rs:369-371`) ⇒ the POST handler (`src/bin/gnosis_server.rs:219`). | **OPEN (2026-09-17, the U3 documentation audit).** `serde_json` **rejects non-finite floats** (`NaN`/`±Inf`), so a `RagResultItem.score` that is not finite makes `to_value` return `Err` and the `expect` **panics** — a panic on a request path instead of a §11-mapped error. **Reachable only through score production:** the server's own legs produce finite scores, but `gnosis_eval` **seeds a `VectorIndex` directly** (its fixtures/harness can carry arbitrary `f32` components), so the path is reachable in-repo today and would become wire-reachable the moment any leg (U5's index build included) can inject a non-finite component. **Pre-existing and out of U3's scope**: U3 touched only the status projection (`src/store/mod.rs:4193-4232`), the lib boot seam and the golden literals — no score-producing code changed, and **no U3 row is affected**. **Disposition: OPEN — owner = the F2 wire foundation / the transport unit** (the fix is a decision, not just a guard: map the serialization failure to a §11 error, or validate `score.is_finite()` at the leg boundary and reject through an existing variant — either defines an outcome and therefore needs its own register row). **Trigger to revisit:** the next unit touching `src/wire/codecs.rs`/`encode_result`/`encode_result_checked`, any unit that can supply a vector component (U5's included), or any consumer report of a panic/500 from `POST /rag/query`. **Cross-filed** in `docs/HANDOFF.md` §"U3 documentation pass (2026-09-17) — engine-side deferred items" as this repo's own work (explicitly **not** an upstream ask). |
| **RBAC-DOC-DRIFT — this repo's own specs described the engine as the RBAC **enforcer** (docs-layer; **9 clauses across 4 spec sections**, of which **8 were stale** and **1 was already correct**)** | The stale clauses (found and annotated 2026-09-22, located **by text** — the line numbers this row originally carried had drifted): `docs/specs/p1a-document-crud-wire.md` §0 (`:60-61` *"the RBAC enforcement semantics (the engine is the enforcer…)"*), §2 (`:112-113`), §4.2/the §8 opening (`:508-509`), §8 (`:522-523` — the stronger *"its value semantics … are engine-enforced"*), §11 (`:651-652`); `docs/specs/p2-gnosis-server.md` §0 (`:479-480`), §2 (`:525`), §8 (`:1745-1746`), §8 (`:1760-1762` — the overstating *"The engine-side RBAC enforcement … is a PACKAGE item, NOT implemented here"*), §12 (`:3074`). **Already correct, left untouched:** `p1a` §10 (`:628-630`, *"P1a pins the shape, not the enforcement"*). | **FIXED (docs-only, 2026-09-22 — the `GRQ-1..GRQ-11` proposal-review gate's accepted **Q4** docs appendix; record: `docs/specs/gnosis-grq-inbound-review.md` §7(v)/§9 step 7/§14 POST-RECORD UPDATE 1).** An **ENGINE-side doc-layer defect — NOT an inbound ask, and not a new `docs/defects.md` row for any `GRQ`** (those stay handoffs per the inbound's own rule 3). The clauses asserted engine-side RBAC *enforcement*; the landed architecture **re-scoped that to the SHELL** — the engine pins only the `caller` **credential shape** plus its presence check in the request-decode layer (a missing `caller` on the 7 mutating methods ⇒ 400), and the `caller` is **not** passed to the store (`src/bin/gnosis_server.rs:117-122`'s own comment + `dispatch`'s store calls, `:124-172`), while the authority mapping and the deny are the shell's. Each stale clause was contradicted by this repo's own later clauses and records (`p1a` §8's re-scope note, `p2` §8's, the `docs/HANDOFF.md` §8-RBAC row, the ACTIVE decision **`GNOSIS-RBAC-EDIT-ENFORCEMENT`** — *"RBAC → SHELL"*). **The CODE already followed the re-scoped ruling and no test asserts either reading, so nothing was ever red** — a prose-only divergence of the `P-3`/`P-7` class. **How it was fixed:** each stale clause is **annotated in place** (historical text retained) with the dated marker *"— RECONCILED 2026-09-22 (`RBAC-DOC-DRIFT`): …"* naming the credential **shape** + decode-layer presence check only and the SHELL-side authority mapping/deny; **no rule, requirement, register row, table or behavior claim changed**, and the pass added **no red set** (justified zero-row exemption — docs-only). **Owner:** the SpecWriter. **Trigger to revisit:** none owed for the prose; the one **follow-on ask** this pass surfaced is recorded separately in `docs/pending.md` ("the four CRUD read routes carry a request body — should they ever stop"), and the `GNOSIS-RBAC-EDIT-ENFORCEMENT` revisit trigger is amended **in the pass that authorizes a durable store** (the gate's R-2), not here. |
| **`U5-ADV-1` — U5 turns a LOUD error into a SILENT wrong answer: the boot index is frozen and nothing ever rebuilds it (host; the adversarial pass's primary escalation)** | `src/store/mod.rs`'s `build_boot_vector_index` (`:5369`) + `src/store/mod.rs:4216` (the read-time `vector == snapshot().vectors.is_some()` — **re-read by gate 8, 2026-09-22: correct**) and the bin's boot composition (`src/bin/gnosis_server.rs:414-419`) — the only installation vehicle is the boot-time `swap_snapshot` (`src/store/mod.rs:1858` — **gate 8: correct**), and **no call site in `src/` ever rebuilds the index** (`build_boot_vector_index`'s only production call site is the boot's `:414`); `snapshot().epoch` is never compared with `store.epoch()` (`:1816`). | **OPEN (2026-09-22, U5's adversarial pass — the spec's residual (ii) accepted "no rebuild after boot", and the pass escalated what that acceptance costs).** After boot, `create_document`/`update_document` change the corpus while the index stays as built, so **`mode=vector` returns 200 with silently missing hits** (`node_snippet` reads an unindexed node as `""`) **while `subsystems.vector` still reports `true`** — whereas **pre-U5 the same request was FS-14 `VectorIndexUnavailable`, a loud 503**. A loud error was replaced by a silent wrong answer: the honesty class the flag-capability semantics exist to prevent. **Fix shape (host):** make the honest signal complete — e.g. `vector == snapshot().vectors.is_some() && snapshot.epoch == self.epoch()` (or a per-snapshot `built_epoch` guard), with a regression test "a document created after boot ⇒ `vector` is not silently `true` (or the read re-derives) and `mode=vector` is **not** a silent empty 200"; alternatively pin the residual explicitly for the consumer. **Owner:** a follow-up unit (its own spec amendment + red set — the shape moves a pinned derivation, so it needs the full SpecWriter/TestWriter/Implementer discipline, not an in-place patch). **Trigger:** the supervisor's/user's disposition, or the first consumer report of missing vector hits after a write. |
| **`U5-ADV-2` — the pre-bind build is unbounded AND un-timed (the accepted residual's stated bound does not exist)** | The build's corpus materialisation + the strictly-sequential `embed` per node, running **before the loopback bind**; the provider client is built with `reqwest::Client::new()` and **no timeout** (`src/bin/gnosis_server.rs:327`). | **OPEN (2026-09-22, U5's adversarial pass; low).** Every embeddable node's text is cloned into a second copy of all node text and one non-batched request is issued per node, with no per-call timeout, no cap and no batched path — a large or hung corpus delays or prevents the bind entirely. The spec's residual (i) claims "the provider's own request timeout is the only bound", and **that bound does not exist** for a hung socket. **Fix shape:** set/pin a real `Client` timeout (a bound that exists) and/or restate the residual honestly; consider streaming the walk instead of materialising the corpus. **Owner:** the follow-up U5-honesty unit or a transport unit. **Trigger:** a consumer-visible boot delay, or the next unit touching the boot/provider client. |
| **`U5-ADV-3` — the failed-`Reachable`-build status contradicts itself (`Degraded` + `embedding:true` + a `last_error` blaming embedding)** | `src/store/mod.rs:4229-4233` (the fixed `last_error` — the literal string is at **`:4230`**; **gate 8, 2026-09-22: the `:4226-4230` spelling this row carried is stale by four lines and is re-pointed**) + the failed-build branch's status application (`src/bin/gnosis_server.rs:425-432`) and its wiring (`:433-437`); pinned as the expected outcome by `P-IM-15` and the conformance layer. | **OPEN (2026-09-22, U5's adversarial pass; recorded as residual (v) in the spec, whose text deliberately filed no row — this row closes that gap).** `state:"Degraded"` with `last_error:"a non-core subsystem (embedding/reranker) is unavailable"` while `subsystems.embedding == true`: the surface blames a subsystem it simultaneously reports as wired. **NOT a U5 code bug** — U5 implements the pinned contract and its register row asserts exactly this pair. **Disposition:** a **spec/consumer decision** — pin a distinct `last_error` for the build-failure branch (needs a §11/V-8.2 golden move in a spec'd unit) or accept the generic sentence. **Owner:** SpecWriter on the supervisor's/user's disposition. |
| **`U5-ADV-4` — the build's error is discarded with no diagnostics (operator-invisible)** | The bin's boot composition discards the build's `Err` (`built.ok().flatten()`, `src/bin/gnosis_server.rs:416-419`; the `Err` is reduced to the boolean `build_ok` at `:415`). | **OPEN (2026-09-22, U5's adversarial pass; low).** An operator cannot distinguish "provider unreachable" from "the provider errored on node 5 000": both render the same generic `Degraded` sentence, and nothing in the repo logs it (there is no logging use anywhere in the boot). **Fix shape:** log the error (stderr) **before** the snapshot composition, keeping the pinned status strings byte-identical. **Owner:** the follow-up U5-honesty unit (small). |
| **`U5-ADV-5` — the `shard.read().unwrap()` panic class now sits on a PRE-BIND path (class-level; not a U5 regression)** | U5's build adds a `shard.read().unwrap()` site to a pre-existing class — **U5's site is `src/store/mod.rs:5386`** (the corpus walk's shard lock; **gate 8, 2026-09-22 — verified**), alongside the pre-existing sites (`:2110`, `:2139`, `:2238`, `:2310`, `:2555`, `:2752`, `:3109`, `:3156`, `:3885`, `:4623`, `:4869`, `:5157`, `:5275`; the `:1822`, `:1852`, `:1859`, `:1865`, `:4196`, `:2590` spellings this row carried are **stale** — most land on unrelated lines, and `:1852`/`:1859` sit in the `snapshot`/`swap_snapshot` block rather than on a lock). | **OPEN (2026-09-22, U5's adversarial pass; low-med).** A poisoned shard lock panics; because the build runs **before the bind**, such a panic aborts the process rather than degrading (the poison path itself is reachable pre-U5 through `delete_document`'s scan). **Not a U5 regression** — U5 adds a site to an existing class. **Fix shape:** a store-wide decision (`unwrap_or_else(|e| e.into_inner())` or an existing error variant) in its own unit; do **not** special-case U5. |
| **`P-9` — TRIGGER FIRED by U5** | `src/wire/codecs.rs:94-99`'s `expect("RagResult is Serialize")`, reached through `encode_result_checked` (`src/wire/query.rs:369-371`) from the POST handler. | **TRIGGER FIRED (2026-09-22, U5's adversarial pass).** P-9's own trigger reads "any unit that can supply a vector component (U5's included)". U5 stores the provider's vectors **verbatim** on the running server's path, so a provider returning `NaN`/`±inf` (a buggy or hostile `EmbeddingProvider`; `cosine` then yields `NaN`) puts a non-finite `RagResultItem.score` into the result and the `expect` **panics on the request path** instead of a §11-mapped error (`validate_rag_result` does not catch it). The shipped `OllamaProvider` cannot itself produce non-finite components (`as_f64().unwrap_or(0.0)`), so reachability requires such a provider. **Disposition unchanged (PACKAGE/FOUNDATION — the F2 wire foundation or the transport unit):** reject non-finite at the leg boundary through an existing variant, or map the serialization failure in `encode_result`; **U5's verbatim clause must NOT be patched** (`P-TP-5` pins it). **Note:** the adversarial pass assigned the TestWriter a negative probe (`T6`) that is EXPECTED to panic today — the panic is the recorded state, not a test bug. |
| **`Ready` does not imply every subsystem flag is `true` (state vs capability axis)** | `src/bin/gnosis_server.rs:391-408` (landed; the pre-U3 citation was `:386-393`, the pre-U1 citation this row carried was `:330-336`) (`Ready` is set on provider reachability alone) + `src/store/mod.rs:1718-1725` (the legacy literal, under its U3 comment block `:1714-1717`; **the `:1714-1725` citation this row carried is stale**), `:4193-4232` (**the derivation site — `get_engine_status`'s read-time projection, the flag vector's only producer**; the pre-U3 citations were `:1710-1717` / `:4181-4195`) | **OPEN — adjudicated as a documented axis split, NOT a code defect (2026-09-16, the U2/U3 register remand, F13; `docs/specs/p2-gnosis-server.md` §9.5.2's notes).** The frozen flag semantics say a flag means "this subsystem's full query-time capability is wired and functional", yet after U3 a boot can report `state: Ready` with `vector: false` (no index until **U5**) and `reranker: false` (no reranker implementation exists anywhere in `src/`). The reviewer asked which axis `Ready` asserts; the pinned answer is that **`EngineState` is a separate (provider/readiness) axis the flag semantics do not scope** — `Ready` asserts that the boot wiring completed, the store/graph/lexical core is up, and an embedding provider was wired and reachable, and it asserts **nothing** about `vector`/`reranker` — so `{Ready, vector:false, reranker:false}` is legitimate and no row asserts "`Ready` ⇒ all flags true". **U3 is explicitly NOT asked to tighten the `Ready` condition**: a tightening would require a built vector index (U5's deliverable) *and* a reranker implementation (which does not exist) for the engine to boot READY at all, i.e. it would make a cold boot permanently non-READY. **Disposition:** document-only; revisit if a future unit lands a reranker or if the shell needs a single "all capabilities up" predicate (the additive `HealthReport` field F2 §9.1 permits is the natural home, and **U3 adds none** — confirmed against the landed unit, 2026-09-17). **U3-verified (2026-09-17):** the split is now **live-observed** — the register's `R-L2` row returned `state:"Ready"` with `vector:false`/`reranker:false` from `GET /engine/status`, so this row's reading is the landed contract, not a forecast. | The frozen flag semantics say a flag means "this subsystem's full query-time capability is wired and functional", yet after U3 a boot can report `state: Ready` with `vector: false` (no index until **U5**) and `reranker: false` (no reranker implementation exists anywhere in `src/`). The reviewer asked which axis `Ready` asserts; the pinned answer is that **`EngineState` is a separate (provider/readiness) axis the flag semantics do not scope** — `Ready` asserts that the boot wiring completed, the store/graph/lexical core is up, and an embedding provider was wired and reachable, and it asserts **nothing** about `vector`/`reranker` — so `{Ready, vector:false, reranker:false}` is legitimate and no row asserts "`Ready` ⇒ all flags true". **U3 is explicitly NOT asked to tighten the `Ready` condition**: a tightening would require a built vector index (U5's deliverable) *and* a reranker implementation (which does not exist) for the engine to boot READY at all, i.e. it would make a cold boot permanently non-READY. **Disposition:** document-only; revisit if a future unit lands a reranker or if the shell needs a single "all capabilities up" predicate (the additive `HealthReport` field F2 §9.1 permits is the natural home, and **U3 adds none**). |
| **A fifth stray vendored consumer-client copy remained: `retrieval.ts` at the repo root** | `retrieval.ts` at the repo root (**59 KB**) — header literally reads `// src/main/retrieval.ts — Unit E: the RAG index + retrieval module` | **FIXED (2026-09-16).** A **fifth** vendored copy of the consumer's TypeScript code: `retrieval.ts` was vendored from the Astrographer shell (its own header names the *consumer's* path, `src/main/retrieval.ts`) and sat at this repo's root in a **Rust-only** tree. It was referenced by **no** file in this repo — there was **no `package.json`**, no `tsconfig`, `scripts/` was empty, and the only textual hit was a prose citation in `docs/research/astrographer-engine-shell-boundary.md:151` that points at the *consumer's* path, not at this copy. **The row's own record of *why* it was a defect is retained:** the copy was the **same ownership/drift question** as the four deleted copies — the consumer repo is the source of truth for that code, so a stale vendored copy in this engine tree documents a client the shell does not actually run. **Fixed on the user's "clean the JS files" instruction (2026-09-16):** the supervisor `git rm`'d `retrieval.ts` together with the two orphaned TypeScript test files (the row below). **Verification:** a follow-up sweep found **no remaining `.ts`/`.js`/`.mjs`/`.tsx`/`.jsx` file anywhere in the tree outside `target/`/`archive/`/`.cargo-home/`**, and the Rust baseline was **unaffected** (`cargo test` **538 pass / 0 fail**). (`.cargo-home/` is a **pre-existing, gitignored, intentional** sandbox-local cargo home (2026-09-10; documented in `.gitignore`) and is **not** JS to clean.) Cross-reference: `docs/specs/gnosis-gr-inbound-review.md` open question 5. |
| **Orphaned vendored TypeScript test files in `tests/` imported modules deleted by the U0 vendored-copy cleanup** | `tests/unit-a1-crud-routing-proxy.test.ts`, `tests/props-a1-crud-routing-proxy.test.ts` | **FIXED (2026-09-16).** Both files were vendored copies of the consumer's A1 CRUD-routing-proxy tests: they statically imported `../src/main/engine-rag-store.js` and `../src/main/engine-crud-rag-store.js` — the four vendored client copies the user approved deleting on 2026-09-16 — so their imports pointed at **removed** files. They were **dead** in this repo: there was **no `package.json`**, no `tsconfig`, no JS test runner and `scripts/` was empty, so nothing executed them (which is why the deletion never reddened the Rust trio). **The row's own record of *why* it was a defect is retained:** orphaned vendored copies of the consumer's tests in a **Rust-only** tree, whose static imports named modules this repo no longer has — a broken, misleading artifact with no runner and no owner. **Fixed on the user's "clean the JS files" instruction (2026-09-16):** the supervisor `git rm`'d both files together with `retrieval.ts` (the row above). **Verification:** a follow-up sweep found **no remaining `.ts`/`.js`/`.mjs`/`.tsx`/`.jsx` file anywhere in the tree outside `target/`/`archive/`/`.cargo-home/`** — the tree is now free of TypeScript/JavaScript — and the Rust baseline was **unaffected** (`cargo test` **538 pass / 0 fail**). |

## FIXED

| Defect | Where fixed | How |
| --- | --- | --- |
| **Stale vendored copies of the consumer's TypeScript client live in this Rust tree** (`engine-rag-store.ts`, `src/main/engine-rag-store.ts`, `engine-crud-rag-store.ts`, `src/main/engine-crud-rag-store.ts`) | `git rm` (2026-09-16) — performed by the supervisor, not by the docs pass | **FIXED (2026-09-16).** The four copies named in the row were **deleted** — `engine-rag-store.ts` + `engine-crud-rag-store.ts` (repo root), `src/main/engine-rag-store.ts` + `src/main/engine-crud-rag-store.ts`, and the then-empty `src/main/` directory. **The row's own record of *why* they were a defect is retained:** four copies of the Astrographer shell's client (`engine-rag-store.ts` and `engine-crud-rag-store.ts`, each twice: at the repo root and under `src/main/`) were vendored into this **Rust-only** engine tree and **drifted from the live consumer client** — the consumer repo is the source of truth for that code, so those copies documented a client that is not the one the shell actually runs (this drift is exactly the class of risk that produced **GR-1**'s bare-body report, where the consumer's own client masks the engine's decode failure via `decodeEnvelope`). **Cross-reference:** open question 5 in `docs/specs/gnosis-gr-inbound-review.md` — **answered: delete** (option A); the gate record's §Validity findings + §Go-ahead record carry the landed state, and the **fifth** copy (`retrieval.ts`) is a separate row, **now FIXED** as well (the two rows below). |
| **A fifth stray vendored consumer-client copy remained: `retrieval.ts` at the repo root** | `git rm` (2026-09-16) of `retrieval.ts` at the repo root — performed by the supervisor, not by the docs pass | **FIXED (2026-09-16).** A **fifth** copy of the consumer's TypeScript code had been vendored from the Astrographer shell — its own header named the *consumer's* path (`// src/main/retrieval.ts — Unit E: the RAG index + retrieval module`) — and it sat at this repo's root in a **Rust-only** tree, referenced by **no** file here (no `package.json`, no `tsconfig`, `scripts/` empty; the only textual hit was a prose citation in `docs/research/astrographer-engine-shell-boundary.md:151` pointing at the *consumer's* path, not at this copy). **The row's own record of *why* it was a defect is retained:** it was the **same ownership/drift question** as the four copies above — a stale vendored consumer client in this engine tree that documents a client the shell does not actually run. **Fixed on the user's "clean the JS files" instruction (2026-09-16)**, together with the two orphaned TypeScript test files (the row below). **Verification:** a follow-up sweep found **no remaining `.ts`/`.js`/`.mjs`/`.tsx`/`.jsx` file anywhere in the tree outside `target/`/`archive/`/`.cargo-home/`**; the Rust baseline was **unaffected** (`cargo test` **538 pass / 0 fail**). (`.cargo-home/` is a **pre-existing, gitignored, intentional** sandbox-local cargo home — 2026-09-10, documented in `.gitignore` — and is **not** JS to clean.) |
| **Orphaned vendored TypeScript test files in `tests/` imported modules deleted by the vendored-copy cleanup** | `git rm` (2026-09-16) of `tests/unit-a1-crud-routing-proxy.test.ts` + `tests/props-a1-crud-routing-proxy.test.ts` — performed by the supervisor, not by the docs pass | **FIXED (2026-09-16).** Both were vendored copies of the consumer's A1 CRUD-routing-proxy tests whose static imports named `../src/main/engine-rag-store.js` and `../src/main/engine-crud-rag-store.js` — the four vendored client copies deleted in the first cleanup pass — so they were orphaned against **removed** files. **The row's own record of *why* it was a defect is retained:** dead vendored test copies in a **Rust-only** tree with no runner and no owner, whose imports pointed at modules this repo no longer has. **Fixed on the user's "clean the JS files" instruction (2026-09-16)**, together with `retrieval.ts` (the row above). **Verification:** a follow-up sweep found **no remaining `.ts`/`.js`/`.mjs`/`.tsx`/`.jsx` file anywhere in the tree outside `target/`/`archive/`/`.cargo-home/`**; the Rust baseline was **unaffected** (`cargo test` **538 pass / 0 fail**). |
| §4.2 `resolve_entities` residual alias entry | `src/store/mod.rs` `resolve_entities` | **FIXED (2026-09-09, decision RESOLVE-ENTITIES-AUTHORITATIVE-OVERWRITE).** `resolve_entities` now re-stabilizes the durable alias→canonical map to an acyclic, flat alias graph on each call: the chosen canonical is never itself an alias (its prior entry is dropped) and every alias points directly to the canonical (prior aliases pointing to a now-re-aliased node are re-pointed via path-compression). A later manual call supersedes an earlier one; the §4.2.9.2 automatic-overwrite invariant is preserved. Same-call idempotence (P-TP-2) holds; overlap convergence is now guaranteed — no residual alias, no cycle. Regression-tested (updated P-TP-2 overlap test + new overlap-flip/chain-flatten tests). |
| §4.1 `list_documents` panics on an out-of-range page / `page: u64::MAX` (overflow + slice OOB) | `src/store/mod.rs` | Clamp: `if start >= total { return empty page }` before any arithmetic/slice; saturating offset. Regression-tested (`list_documents_page_beyond_range_...`, `list_documents_page_u64_max_...`). |
| §4.1 `delete_document` delete-gate TOCTOU — a reference added between the scan and the remove leaves a dangling reference | `src/store/mod.rs` | Store-wide `reference_lock: RwLock<()>`: delete takes `write()` for the whole scan+remove; edge-adding mutations (`create`/`update_document`) take `read()`. Acquired before shard locks, fixed order → no deadlock. |
| §4.1 publish gate ignored `crosslink` edges in `Broken`/`Stale` | `src/store/mod.rs` | Gate now matches `link`/`embed`/`crosslink` uniformly: `BROKEN || STALE ⇒ UnresolvedReference`. Regression-tested (`publish_document_with_{broken,stale}_crosslink_...`). |
| §4.1 publish gate trusted a caller-supplied reference `state` (e.g. fabricated `Resolved` to a nonexistent target) | `src/store/mod.rs` | Publish derives reference state from target existence: a missing/archived target ⇒ `Broken` ⇒ `UnresolvedReference`; cross-wiki edges skip local verification. Regression-tested (`publish_document_with_resolved_link_to_nonexistent_target...`). |
| §4.1 `concurrent_*` tests ran on a single-threaded runtime with no `.await` → false security | `tests/store_integration.rs` | Converted to `#[tokio::test(flavor="multi_thread", worker_threads=4)]` with a `tokio::sync::Barrier`; validated 5× at `--test-threads=4`. |
| §4.1 WRITER-ACTOR-JOURNAL (ACTIVE) not honored — mutations were direct, no journal/epoch feed | `src/store/mod.rs` | Added minimal `MutationJournal` (`RwLock<Vec<JournalEntry>>` + `AtomicU64 epoch`) appended in each mutation critical section + `Store::epoch()`/`journal_len()` accessors, honoring the single-writer/epoch-feed intent for the §4.2–§4.4 index-rebuild/audit layers. |
| §4.3 `delete_document` left dangling fact citations (gate scanned only edges, not `fact_store`) + a commit-time grounding TOCTOU | `src/store/mod.rs` | Delete gate scans `fact_store` for citing facts → `DocumentInUse`; fact commits take `reference_lock.read()` and re-verify grounding inside `fact_store.write()`. Regression-tested. |
| §4.3 `update_fact` accepted empty/whitespace value | `src/store/mod.rs` | Trim → `ValidationError` before applying. Regression-tested. |
| §4.3 whitespace-only `factKey`/`value` passed schema conformance (`.is_empty()` only) | `src/store/mod.rs` | Trim before `is_empty` in `create_fact` + `propose_candidate_fact` gate. Regression-tested. |
| §4.3 unknown-wiki handling inconsistent/absent on the fact surface | `src/store/mod.rs` | `WikiNotFound` (FS-2) up front on `create_fact`/`update_fact`/`propose_candidate_fact`/`get_fact`. Regression-tested. |
| §4.3 test-impl doc mismatch: header claimed the gate handled cross-field consistency | `tests/facts_integration.rs` (+ §4.5) | Cross-field consistency declared **deferred to §4.5** (needs the embedding leg); header corrected; no fake branch. |
| §4.3/§4.5 cross-field consistency gate: not enforced by the runtime gate | `src/store/mod.rs` `propose_candidate_fact` | **CLOSED as a documented non-goal (2026-09-09).** The §4.3 gate enforces schema/grounding/dedup only (`rejected_outcome` invoked only with `code:"schema"`; no `cross-field`/`CrossField` branch in `src/`). Enforcing "value consistent with cited nodes' content" needs an LLM/embedding judgment on the commit path, breaking the deterministic fail-closed gate framing (§4.3.2a) and injecting a provider dependency into the manual-override flow (§4.3.2a.3). Reconcile spec §4.3.2a.1 to mark cross-field as aspirational/offline, not a runtime §4.3 branch. See `docs/pending.md`. |
| §4.3/§4.5 fact `node_id` coherence: stored `node_id` is a `fact-{fact_key}` handle, not a live graph `fact` node | `src/store/mod.rs` `create_fact`/`propose_candidate_fact` | **CLOSED as a spec-vs-impl wording tension / spec-reconcile (2026-09-09).** §4.5.4 makes `factKey` the canonical retrieval identity; `(documentId, nodeId)` is a derived conventional handle, not the live graph node (which only exists when the caller authored it). Engine will **not** auto-materialize fact nodes on commit (breaks frozen `Node`/`Fact` model + manual-override convention + pinned tests). Reconcile contract §4.3.1 wording. See `docs/pending.md` + `docs/HANDOFF.md`. |
| §4.3.4 engine-side query audit-log: previously `Ok(vec![])` | `src/store/mod.rs` | **RESOLVED / landed via §4.5 (2026-09-09).** `rag_query` appends a real `QueryAuditEntry` per committed query and `rag_stream` routes through it; `get_query_audit_log` returns real entries. Pinned by `facts_integration.rs::get_query_audit_log_returns_audit_entry_shape` + `rag_query_integration.rs::rag_query_appends_query_audit_entries`. |
| §4.4 `publish_document` AB-BA deadlock (shard-write → `fact_store`/foreign-shard read vs. fact-commit `fact_store`-write → shard read) | `src/store/mod.rs` | Uniform lock order `reference_lock → shard → fact_store/communities/journal` (decision LOCK-ORDER-REF-SHARD-SIDECAR); publish validates under read locks then takes the single shard write only for the transition; fact-commits ground under `reference_lock.read()` before `fact_store.write()`. |
| §4.4 propagation TOCTOU — embeds created after a fact-update scan could stay `FRESH` with a stale snapshot | `src/store/mod.rs` | Derive embed state from snapshot-vs-canonical on read/write so a post-scan stale embed reports `STALE`. |
| §4.4 reference target-**change** (repoint) not propagated — `update_document` stored caller `state` verbatim | `src/store/mod.rs` | Re-stamp reference-edge state from the NEW target's liveness (missing/archived → BROKEN/STALE; live → RESOLVED/FRESH-vs-STALE); cross-wiki left unverifiable. |
| §4.4 `propagate_fact_staleness`/`propagate_archived_target` didn't bump `revision` or journal referencing docs | `src/store/mod.rs` | Bump referencing doc `revision` + append journal entry (epoch bump); states preserved. |
| §4.4 `get_consistency_report` fabricated state for `None`-state edges + wrong crosslink default (`Fresh` for a live link) | `src/store/mod.rs` | Derive `None`-state edges from target liveness; crosslink-with-snapshot = embed (FRESH/STALE), crosslink-without = live link (RESOLVED). |
| §4.4 `re_sync_embed` incoherent snapshot (read canonical before shard write) + non-fact embed targets | `src/store/mod.rs` | Re-sync reads the canonical value under proper locking; non-fact targets resolve canonical from the target node's value. |
| §4.5 LOW-G test/spec mismatch — `binaryCandidatePool` default 10×topK | `tests/retrieval_stack_integration.rs` `binary_candidate_pool_none_default_caps_pool_at_tenx_topk` + `src/store/mod.rs` | **FIXED (reconciled).** The engine keeps the spec-faithful default `binaryCandidatePool = 10 × top_k` (`options.binary_candidate_pool.unwrap_or(10 * top_k)`, §4.5.3a.3). The reconciliation was done by **amending the test seed** exactly as the earlier OPEN note prescribed: the test now seeds `DEFAULT_CAP`(=20 = 10×topK for `top_k = 2`) coarser Hamming-0 candidates **plus `WORSE`(=2) more** for 22 total, so the true full-cosine best sits at binary position 21 — **beyond** the 20-wide 10×topK pool — and the default cap correctly excludes it. `retrieval_stack` is 18/18 green. (Test-file doc-note: the `binary_candidate_pool_none_default_caps_pool_at_tenx_topk` header still carries a stale "RED today…" preamble describing the old `0`-means-uncapped behavior; that is a comment-only clean-up outside the docs tree.) |
| §7.2 F2 blind-greens RED — `decode_rag_result` precedence for a body that is *both* malformed and trace-less (`{"query":123}`) | `src/wire/decode.rs` (`decode_rag_result`) | **RESOLVED as a contract-precedence DOC-fix, not a code defect (2026-09-09).** The blind-greens RED observed `{"query":123}` → `Err(MissingTrace)` → `TraceUnavailable` where the contract's §7/§13/V-9 wording had claimed "wrong field type → `InvalidJson` → `EngineError`". The implementer's trace-presence-first precedence is the correct, FS-10-preserving behavior; the **contract** was tightened (not the code) to pin it — `docs/specs/engine-wire-contract.md` §7 (decode_rag_result documents "trace-`key`-presence is checked FIRST … a malformed-and-traceless body stays FS-10") and §12 V-9 **"Precedence (pinned)"** — and `src/wire/decode.rs:50-52` verifiably returns `MissingTrace` when no `trace` key exists, before any structural deserialization. Both `trace_unavailable` and `engine_error` map to HTTP **502** (§11), so the rendered status is unchanged. **No code-defect row is warranted** — this is a documentation/precedence reconciliation, recorded here as resolved (and in the F2 DONE row + `docs/greens/7-2-wire-greens.md` RED annotation). |
