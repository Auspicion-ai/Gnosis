# Gnosis — Active Defect / Requirement-Gap List

Maintained by the document-archival loop. Open gaps on top; fixed rows below.
A defect in a foundation/package (or a gap owned by another suite tool) is a
HANDOFF item — never patched here. A host-side finding (this project's `src/`)
is fixed here.

## OPEN

| Defect / gap | Where it stands | Note |
| --- | --- | --- |
| **`subTaskDag` is a validated-only no-op** | `src/store/mod.rs` (option parsed + validated; nothing decomposed) | **DOCUMENTED, NOT IMPLEMENTED** (decision SUB-TASK-DAG-VALIDATED-ONLY). The inference-time sub-task DAG is opto-in / SHOULD-HAVE (like adaptive RAG, which is PARKED). A single-node decomposition (the query itself) is the honest stand-in; **no fake decomposition** is fabricated. `SubTaskDagFailed` is a **RESERVED** variant. Parked in `docs/pending.md`. |
| **`CompressionFailed` is reserved (no fail-state)** | `src/store/mod.rs` (compressor is total) | The spec says the compressor **degrades gracefully to uncompressed** on failure; the local compressor (Filter/Extract/Graph) only **transforms** and never fails-and-degrades. `CompressionFailed` is a **RESERVED** variant for a future non-degradable compressor leg. The graceful-degrade behavior is **spec-correct**; this is **not** a missing fail-state (decision RESERVED-ERRVARIANTS-DISCIPLINE). |
| **`HyDEGenerationFailed` reserved; provider errors surface as `EmbeddingUnavailable` (LOW-E)** | `src/store/mod.rs` `vector_query` | `generate_hypothetical` is **total** (a deterministic stand-in), so it never constructs `HyDEGenerationFailed`. The prior code **relabelled** any `embed` provider error as `HyDEGenerationFailed`; fixed (LOW-E) so an embedding-provider failure surfaces as `EmbeddingUnavailable`. `HyDEGenerationFailed` remains **reserved** for the actual hypothetical-**generation** step when it becomes fallible. |
| **§4.4 `publish_document` concurrent-atomicity non-goal (TOCTOU)** | `src/store/mod.rs` `publish_document` | Publish validates target-liveness **under READ locks** but does **not** re-run the liveness/stale check under the shard write for the `DRAFT→PUBLISHED` transition, so a concurrent target mutation between validation and the transition can yield a publish with a later-**stale** reference. This is a documented, deliberately-***excluded*** non-goal of the §4.4 PBT register's `P-TP-2` (decision note), **NOT** asserted as an invariant. **Disposition:** engine-internal deferred — revisit at the F2/IPC seam pass if publish is used under contended writes. |

## FIXED

| Defect | Where fixed | How |
| --- | --- | --- |
| §4.2 `resolve_entities` residual alias entry | `src/store/mod.rs` `resolve_entities` | **FIXED (2026-09-09, decision RESOLVE-ENTITIES-AUTHORITATIVE-OVERWRITE).** `resolve_entities` now re-stabilizes the durable alias→canonical map to an acyclic, flat alias graph on each call: the chosen canonical is never itself an alias (its prior entry is dropped) and every alias points directly to the canonical (prior aliases pointing to a now-re-aliased node are re-pointed via path-compression). A later manual call supersedes an earlier one; the §4.2.9.2 automatic-overwrite invariant is preserved. Same-call idempotence (P-TP-2) holds; overlap convergence is now guaranteed — no residual alias, no cycle. Regression-tested (updated P-TP-2 overlap test + new overlap-flip/chain-flatten tests). |
| §4.1 `list_documents` panics on an out-of-range page / `page: u64::MAX` (overflow + slice OOB) | `src/store/mod.rs` | Clamp: `if start >= total { return empty page }` before any arithmetic/slice; saturating offset. Regression-tested (`list_documents_page_beyond_range_...`, `list_documents_page_u64_max_...`). |
| §4.1 `delete_document` delete-gate TOCTOU — a reference added between the scan and the remove leaves a dangling reference | `src/store/mod.rs` | Store-wide `reference_lock: RwLock<()>`: delete takes `write()` for the whole scan+remove; edge-adding mutations (`create`/`update_document`) take `read()`. Acquired before shard locks, fixed order → no deadlock. |
| §4.1 publish gate ignored `crosslink` edges in `Broken`/`Stale` | `src/store/mod.rs` | Gate now matches `link`/`embed`/`crosslink` uniformly: `BROKEN || STALE ⇒ UnresolvedReference`. Regression-tested (`publish_document_with_{broken,stale}_crosslink_...`). |
| §4.1 publish gate trusted a caller-supplied reference `state` (e.g. fabricated `Resolved` to a nonexistent target) | `src/store/mod.rs` | Publish derives reference state from target existence: a missing/archived target ⇒ `Broken` ⇒ `UnresolvedReference`; cross-wiki edges skip local verification. Regression-tested (`publish_document_with_resolved_link_to_nonexistent_target...`). |
| §4.1 `concurrent_*` tests ran on a single-threaded runtime with no `.await` → false security | `tests/store_integration.rs` | Converted to `#[tokio::test(flavor="multi_thread", worker_threads=4)]` with a `tokio::sync::Barrier`; validated 5× at `--test-threads=4`. |
| §4.1 WRITER-ACTOR-JOURNAL (ACTIVE) not honored — mutations were direct, no journal/epoch feed | `src/store/mod.rs` | Added minimal `MutationJournal` (`RwLock<Vec<JournalEntry>>` + `AtomicU64 epoch`) appended in each mutation critical section + `Store::epoch()`/`journal_len()` accessors, honoring the single-writer/epoch-feed intent for the §4.2–§4.4 index-rebuild/audit layers. |
| §4.3 `delete_document` left dangling fact citations (gate scanned only edges, not `fact_store`) + a commit-time grounding TOCTOU | `src/store/mod.rs` | Delete gate scans `fact_store` for citing facts → `DocumentInUse`; fact commits take `reference_lock.read()` and re-verify grounding inside `fact_store.write()`. Regression-tested. |
| §4.3 `update_fact` accepted empty/whitespace value | `src/store/mod.rs` | Trim → `ValidationError` before applying. Regression-tested. |
| §4.3 whitespace-only `factKey`/`value` passed schema conformance (`.is_empty()` only) | `src/store/mod.rs` | Trim before `is_empty` in `create_fact` + `propose_candidate_fact` gate. Regression-tested. |
| §4.3 unknown-wiki handling inconsistent/absent on the fact surface | `src/store/mod.rs` | `WikiNotFound` (FS-2) up front on `create_fact`/`update_fact`/`propose_candidate_fact`/`get_fact`. Regression-tested. |
| §4.3 test-impl doc mismatch: header claimed the gate handled cross-field consistency | `tests/facts_integration.rs` (+ §4.5) | Cross-field consistency declared **deferred to §4.5** (needs the embedding leg); header corrected; no fake branch. |
| §4.3/§4.5 cross-field consistency gate: not enforced by the runtime gate | `src/store/mod.rs` `propose_candidate_fact` | **CLOSED as a documented non-goal (2026-09-09).** The §4.3 gate enforces schema/grounding/dedup only (`rejected_outcome` invoked only with `code:"schema"`; no `cross-field`/`CrossField` branch in `src/`). Enforcing "value consistent with cited nodes' content" needs an LLM/embedding judgment on the commit path, breaking the deterministic fail-closed gate framing (§4.3.2a) and injecting a provider dependency into the manual-override flow (§4.3.2a.3). Reconcile spec §4.3.2a.1 to mark cross-field as aspirational/offline, not a runtime §4.3 branch. See `docs/pending.md`. |
| §4.3/§4.5 fact `node_id` coherence: stored `node_id` is a `fact-{fact_key}` handle, not a live graph `fact` node | `src/store/mod.rs` `create_fact`/`propose_candidate_fact` | **CLOSED as a spec-vs-impl wording tension / spec-reconcile (2026-09-09).** §4.5.4 makes `factKey` the canonical retrieval identity; `(documentId, nodeId)` is a derived conventional handle, not the live graph node (which only exists when the caller authored it). Engine will **not** auto-materialize fact nodes on commit (breaks frozen `Node`/`Fact` model + manual-override convention + pinned tests). Reconcile contract §4.3.1 wording. See `docs/pending.md` + `docs/HANDOFF.md`. |
| §4.3.4 engine-side query audit-log: previously `Ok(vec![])` | `src/store/mod.rs` | **RESOLVED / landed via §4.5 (2026-09-09).** `rag_query` appends a real `QueryAuditEntry` per committed query and `rag_stream` routes through it; `get_query_audit_log` returns real entries. Pinned by `facts_integration.rs::get_query_audit_log_returns_audit_entry_shape` + `rag_query_integration.rs::rag_query_appends_query_audit_entries`. |
| §4.4 `publish_document` AB-BA deadlock (shard-write → `fact_store`/foreign-shard read vs. fact-commit `fact_store`-write → shard read) | `src/store/mod.rs` | Uniform lock order `reference_lock → shard → fact_store/communities/journal` (decision LOCK-ORDER-REF-SHARD-SIDECAR); publish validates under read locks then takes the single shard write only for the transition; fact-commits ground under `reference_lock.read()` before `fact_store.write()`. |
| §4.4 propagation TOCTOU — embeds created after a fact-update scan could stay `FRESH` with a stale snapshot | `src/store/mod.rs` | Derive embed state from snapshot-vs-canonical on read/write so a post-scan stale embed reports `STALE`. |
| §4.4 reference target-**change** (repoint) not propagated — `update_document` stored caller `state` verbatim | `src/store/mod.rs` | Re-stamp reference-edge state from the NEW target's liveness (missing/archived → BROKEN/STALE; live → RESOLVED/FRESH-vs-STALE); cross-wiki left unverifiable. |
| §4.4 `propagate_fact_staleness`/`propagate_archived_target` didn't bump `revision` or journal referencing docs | `src/store/mod.rs` | Bump referencing doc `revision` + append journal entry (epoch bump); states preserved. |
| §4.4 `get_consistency_report` fabricated state for `None`-state edges + wrong crosslink default (`Fresh` for a live link) | `src/store/mod.rs` | Derive `None`-state edges from target liveness; crosslink-with-snapshot = embed (FRESH/STALE), crosslink-without = live link (RESOLVED). |
| §4.4 `re_sync_embed` incoherent snapshot (read canonical before shard write) + non-fact embed targets | `src/store/mod.rs` | Re-sync reads the canonical value under proper locking; non-fact targets resolve canonical from the target node's value. |
| §4.5 LOW-G test/spec mismatch — `binaryCandidatePool` default 10×topK | `tests/retrieval_stack_integration.rs` `binary_candidate_pool_none_default_caps_pool_at_tenx_topk` + `src/store/mod.rs` | **FIXED (reconciled).** The engine keeps the spec-faithful default `binaryCandidatePool = 10 × top_k` (`options.binary_candidate_pool.unwrap_or(10 * top_k)`, §4.5.3a.3). The reconciliation was done by **amending the test seed** exactly as the earlier OPEN note prescribed: the test now seeds `DEFAULT_CAP`(=20 = 10×topK for `top_k = 2`) coarser Hamming-0 candidates **plus `WORSE`(=2) more** for 22 total, so the true full-cosine best sits at binary position 21 — **beyond** the 20-wide 10×topK pool — and the default cap correctly excludes it. `retrieval_stack` is 18/18 green. (Test-file doc-note: the `binary_candidate_pool_none_default_caps_pool_at_tenx_topk` header still carries a stale "RED today…" preamble describing the old `0`-means-uncapped behavior; that is a comment-only clean-up outside the docs tree.) |
| §7.2 F2 blind-greens RED — `decode_rag_result` precedence for a body that is *both* malformed and trace-less (`{"query":123}`) | `src/wire/decode.rs` (`decode_rag_result`) | **RESOLVED as a contract-precedence DOC-fix, not a code defect (2026-09-09).** The blind-greens RED observed `{"query":123}` → `Err(MissingTrace)` → `TraceUnavailable` where the contract's §7/§13/V-9 wording had claimed "wrong field type → `InvalidJson` → `EngineError`". The implementer's trace-presence-first precedence is the correct, FS-10-preserving behavior; the **contract** was tightened (not the code) to pin it — `docs/specs/engine-wire-contract.md` §7 (decode_rag_result documents "trace-`key`-presence is checked FIRST … a malformed-and-traceless body stays FS-10") and §12 V-9 **"Precedence (pinned)"** — and `src/wire/decode.rs:50-52` verifiably returns `MissingTrace` when no `trace` key exists, before any structural deserialization. Both `trace_unavailable` and `engine_error` map to HTTP **502** (§11), so the rendered status is unchanged. **No code-defect row is warranted** — this is a documentation/precedence reconciliation, recorded here as resolved (and in the F2 DONE row + `docs/greens/7-2-wire-greens.md` RED annotation). |
