# Gnosis — Active Decisions Summary

Maintained by the document-archival loop. ACTIVE = governs the current Gnosis
architecture; SUPERSEDED = replaced (provenance only). The four design
constraints (D1–D4) are the suite's constitution — every proposal is reviewed
through them.

## Imported suite decisions (from `../Auspicion Suite/docs/decisions.md`)

| Decision | What it pins |
| --- | --- |
| **GNOSIS-ENGINE** | Gnosis is the production graph/vector engine; Incanter is ARCHIVED as a prototype lexical system. |
| **INCANTER-DISPOSITION** | The production engine is non-JS (Rust); Incanter was the prototype (now archived). |
| **D1–D4** | open-source first (AGPL-3.0), local-first, interconnection, MCP-GUI parity (at the shell). |
| **MCP-SURFACE-DEFERRED** | The suite-wide MCP surface stays PROVISIONAL until each tool's implementation pass. |
| **D4-CARVEOUT-SCOPE** | The D4 security-configuration carve-out (GUI-only) minimum set + per-tool additions. |
| **GRAPH-PUSH-FORMAT** | `provident-graph/1` is the canonical push contract. |
| **FAMILIAR-MEMORY-STORE-INTEGRATION** | Familiar can OPTIONALLY use Gnosis (via Astrographer) as its memory store. |

## Gnosis-local decisions

**LANDED AS ACTIVE (2026-09-16, gate-1 review of the inbound GR-1..GR-9 set —
`docs/specs/gnosis-gr-inbound-review.md`).** The gate's two proposed decisions are **now ACTIVE rows in the
table below**: **`GNOSIS-CHANGE-CURSOR`** — the engine exposes exactly one wire-visible change cursor
(an opaque string equal to the committed journal `seq`, advancing 1 per committed journal entry,
process-lifetime monotonic, explicitly **not** point-in-time and **not** valid for cache validation,
optimistic concurrency, or state reconstruction; per-document `revision` stays the concurrency token) and
**never** a consumer-visible store-wide revision (`/snapshot?revision=` + `stale_revision` are refused);
and **`SUBSYSTEM-FLAG-CAPABILITY-SEMANTICS`** — an `/engine/status` subsystem flag means "this subsystem's
full query-time capability is wired and functional for the current store", which makes the six hard-coded
`true` `EngineSubsystems` flags a defect (see the OPEN row in `docs/defects.md`). They became ACTIVE on the
user's 2026-09-16 instruction **"clean the JS files and then proceed"**, which extended the go-ahead to
**U1** — the recommendation was **accepted by proceeding** (the user did not select the alternatives
option-by-option). Both are now in force as **landed contract for U1** — **U1 LANDED 2026-09-16**
(its spec-gate reviewer loop returned **EMPTY ⇒ the spec gate is OPEN**; pins in
`docs/specs/p2-gnosis-server.md` §5.3–§5.9 + `docs/specs/engine-wire-contract.md`
§4.5–§4.7/§9.1/§16) — **SUPERSEDED (2026-09-16) as to U2/U3: the user authorized U2 and U3, and their
typed §5.x registers are now AUTHORED (`docs/specs/p2-gnosis-server.md` §9.5.1 U2 = 7 rows / §9.5.2 U3 = 5
rows, with the execution plan §9.5.3); the code-bearing units that realize the two decisions are therefore
U2/U3 (AUTHORIZED, code still owed) and U4/U5 remain NOT authorized.** **UPDATE (2026-09-17, proofread pass):
U2's code has since LANDED REALIZED-GREEN** (`cargo test` 582 / 0, U2's property layer 400 cases HELD, blind
set 24/24, the live battery's `R-L1` PASSED live — `docs/specs/p2-gnosis-server.md` §U1's U2-landed bullet), so
the "code still owed" clause above now applies to **U3 only**; **U4/U5 remain NOT authorized**. The historical clause read: the
code-bearing units that
realize them (**U4** for the cursor, **U3** for the flag semantics) remain **NOT authorized**.
**UPDATE (2026-09-22, the U5 gate-8 doc review): the U5 half of every "U4/U5 NOT authorized" clause in this
preamble is superseded — U5 WAS AUTHORIZED (2026-09-22, the `GRQ` go-ahead) and has since **LANDED-GREEN** with
all eight gates run (`docs/next-steps.md` §DONE's U5 row + `archive/reviews/2026-09-22-u5-boot-vector-index-doc-review.md`),
so the boot index build / the one-value `vector:true` flip is **landed**, not owed. U4 alone remains NOT
authorized.**

**Post-greens doc-review check (2026-09-16, docs-only) — no decision row claims U2's code is owed.** Both
decision rows' "what it pins"/source cells were re-read against the landed tree: `GNOSIS-CHANGE-CURSOR`'s owed
code is the **cursor accessor + `GET /changes` + the paged reads = U4's** (not U2's) and
`SUBSYSTEM-FLAG-CAPABILITY-SEMANTICS`'s is **U3's** flag derivation; the only clause that ever read
"U2/U3 … code still owed" is the **preamble paragraph above**, and its 2026-09-17 update already narrows it to
**U3 only** (U2's code LANDED — `docs/next-steps.md`'s U2 DONE row). No row text was changed by this check; no
ACTIVE decision was added, removed or re-scoped. **U3 post-greens doc-review (2026-09-17): the same reading
now applies to *both* units** — **U2's and U3's code have both LANDED** (U2 realized-green 2026-09-17; U3
LANDED-GREEN 2026-09-17), so **no `code still owed` clause survives for either**, and the only
not-authorized code-bearing units are **U4/U5** (`GNOSIS-CHANGE-CURSOR`'s cursor accessor + `GET /changes` +
the paged reads, and `SUBSYSTEM-FLAG-CAPABILITY-SEMANTICS`'s remaining boot index build / the one-value
`vector:true` flip). The historical clause above stands as the 2026-09-16/17 record; the two ACTIVE decision
rows themselves are unchanged (their semantics are landed as contract and as code), and the durability
**direction-only** line below keeps its own gating. **UPDATE (2026-09-22, gate 8): the U5 half of this clause is
superseded — U5 was authorized on 2026-09-22 and LANDED-GREEN (the boot index build + the `vector:true` flip are
landed), so **U4 is the only not-authorized code-bearing unit**; the clause stands as the 2026-09-17 record.**

- **`ENGINE-DURABLE-CORPUS-DIRECTION` — DIRECTION ONLY / NOT ACTIVE (2026-09-16, recording the user's
  answer to gate open question 4).** The user answered **YES**: the engine **will eventually own a durable
  corpus**, and a **durability design unit is on the roadmap** (scope: format / atomic commit / crash
  recovery / store-scope revision semantics — see the GR-7 row in `docs/pending.md`). It is **NOT
  authorized to start** (the approved scope was **U0+U1**, both now landed — U1 on 2026-09-16, its spec-gate
  review EMPTY ⇒ the spec gate is open — with **U2/U3 now AUTHORIZED (2026-09-16; their §9.5 registers are
  authored), while U4/U5 are held**; the historical clause read "**U2–U5 held**"; **gate-8 update, 2026-09-22:
  U5 is no longer held — it was authorized and LANDED-GREEN, so the sentence reads "U4 is held"**) and it remains **gated
  on the consumer's `SINGLE-WRITER-STORE` (O-8) amendment being recorded** plus a scheduled consumer unit.
  This is a recorded **roadmap direction**, not a decision in force: **no ACTIVE row is added for it**, and
  nothing here may be cited as authority for starting the design unit. **RE-READ 2026-09-22 (the
  `GRQ-1..GRQ-11` proposal-review gate):** the direction, its NOT-ACTIVE status and its **3-conjunct trigger are
  UNCHANGED** by the inbound re-filing (which labels the item `OWED` while this repo's status word is **PARKED**);
  the gate **adds the durable-authority fork** as a **user question that must be answered before this unit may be
  authorized** (`docs/specs/gnosis-grq-inbound-review.md` §7(i) / §13 Q1 / §15.5 condition 2) and requires the
  `GNOSIS-RBAC-EDIT-ENFORCEMENT` revisit trigger (the ACTIVE row below) to be amended **in the same pass that
  authorizes a durable store** (§15.3 R-2). **No row in this file changes today.** **POST-GO-AHEAD (2026-09-22):
  the user answered the gate's Q1 as (A) — the engine BECOMES the durable authority (the shell becomes a
  consumer; the consumer's `SINGLE-WRITER-STORE`/O-8 amendment is the precondition) — and answered Q2 as (B):
  the design unit is **NOT authorized** until that O-8 amendment is recorded consumer-side. So this line's
  DIRECTION-ONLY / NOT-ACTIVE status is **unchanged**, its trigger is **unchanged**, and the answer is recorded
  here as the direction's own provenance (no ACTIVE row is added — nothing is in force).

| Decision | Date | What it pins |
| --- | --- | --- |
| **ARCHIVE-GITIGNORED** | 2026-09-09 | The `archive/` directory is **GITIGNORED** per the archival-loop convention (obsolete docs, stale test data, findings reports, and historical review records move there and are never committed). Content there is local provenance, not part of the tracked repo. |
| **LOCKFILE-COMMITTED** | 2026-09-09 | Cargo.lock is **committed** for this binary/lib crate so build reproducibility is pinned for consumers of the engine. |
| **SHARDED-RWLOCK-STORE** | 2026-09-09 | The authoritative store is a **sharded `RwLock` map** keyed by `(wikiId, documentId)`. Reads take per-shard read locks (many concurrent); a document's mutation holds only that shard's write lock. Optimistic concurrency (§4.1.4) — compare-base-revision → apply → bump — is atomic under the shard write lock. No global store lock. |
| **IMMUTABLE-DERIVED-SNAPSHOT** | 2026-09-09 | All derived indexes (lexical BM25, multi-field vector index, triple index, profile summary) are **immutable `Arc<Snapshot>`s swapped atomically** (behind `RwLock<Arc<T>>` / arc-swap). Query-time reads are lock-free on the snapshot; a writer rebuilds and swaps on the epoch/dirty feed. Honors spec §4.2.7.2 "derived index is not a second owner". |
| **WRITER-ACTOR-JOURNAL** | 2026-09-09 | All mutations route through a **single-writer journal actor** (append-only mutation journal + separate query audit log). A single owner mutates; everything else reads. Epoch/dirty counter drives index rebuilds in batches (not per-write). Resolves the mutable-—multi-thread tension by partitioning mutation to one owner. **Applied (2026-09-09, §4.1):** a minimal `MutationJournal` (`RwLock<Vec<JournalEntry>>` + `AtomicU64 epoch`) is appended in each mutation critical section with `Store::epoch()`/`journal_len()` accessors; a tokio writer-actor is held off until §4.2–§4.4 need the async index-rebuild/audit feed and the write-path latency it can add is justified. |
| **LAYERED-OVERLAY** | 2026-09-09 | Live updates are **layered over a revisioned base**: overlay-first reads (pending `OverlayLayer` for an agent/workspace/transaction scope), commit flattens into the authoritative store under the shard lock. Journal preserves prior values for rollback/provenance/reconstruct. Manual declarations are authoritative and never overwritten by automatic extraction (§4.2.8.4). |
| **ARC-SHARED-ENGINE** | 2026-09-09 | The engine is an **`Arc<Gnosis>`** shared across tokio tasks (the `ragQuery`/`ragStream`/`getEngineStatus` seam + `RagStore` seam). Reads use the immutable snapshot; mutation serializes at the per-document shard or the journal actor — never a global state lock, so many concurrent requests do not block each other. |
| **GRAPH-OWNS-RELATION-AND-MERGE** | 2026-09-09 | §4.2's **adversarial** findings force a type evolution: the graph types **own** the data the spec pins rather than a sidecar. `Edge` gains `relation_type: Option<String>` (so a `relation` edge genuinely carries its `relationType`, §4.2.7.2 — a triple is read from the graph's relation edges, not a `triple_store` second-source-of-truth); node deletion **prunes** the relation edges (real cascade, §4.2.7.5); `merge_facts` **persists** the merged fact (union citations + `updated_at` + journal, §4.2.9.1); `resolve_entities` writes a durable alias→canonical mapping (jourled, §4.2.9.1); `set_reference_state` mutates only the matched edge's state under `reference_lock` with an optimistic-concurrency base compare (no whole-document clobber); communities are graph-owned node+`member`-edge structures with member existence validated (§4.2.8.3). This supersedes the earlier "frozen §4.1 types / sidecar-projection" approach the §4.2 TestWriter used, which the adversarial gate rejected as a second source of truth. |
| **LOCK-ORDER-REF-SHARD-SIDECAR** | 2026-09-09 | §4.4's adversarial gate found an AB-BA deadlock and dictated a **single uniform lock order** across the engine: **`reference_lock` → shard (read/write) → `fact_store`/communities/journal**. No path holds a shard **write** while acquiring another shard or `fact_store`; no path holds `fact_store.write()` while acquiring a shard (fact-commit paths ground citations under `reference_lock.read()` before taking `fact_store.write()`); `publish_document` validates the whole gate under read locks then takes the single shard write only for the state transition, re-validating the doc's revision (concurrent change → `ConflictError`/retry). This is the concurrency contract every future unit (esp. §4.5 retrieval) must honor. |
| **SUB-TASK-DAG-VALIDATED-ONLY** | 2026-09-09 | The inference-time `subTaskDag` option is **opto-in / SHOULD-HAVE** (spec's own framing, alongside adaptive RAG, which is PARKED). Currently it is parsed + validated only: it decomposes **nothing** — a single-node decomposition (the query itself) is the honest stand-in, **no fake decomposition is fabricated**. `SubTaskDagFailed` is a **RESERVED** variant (throwable once a real DAG decomposition lands). The full sub-task DAG is PARKED (engine-internal deferred), recorded under `docs/pending.md`. No test requires it to throw. |
| **RESERVED-ERRVARIANTS-DISCIPLINE** | 2026-09-09 | A fail-state variant is only **constructed when its step genuinely runs and fails**; a variant whose generating step is currently **total** (cannot fail) or **not implemented** is documented as **RESERVED**, never fabricated into a throw-path. Applies to `HyDEGenerationFailed` (LOW-E — `generate_hypothetical` is total, so a provider embed failure surfaces as `EmbeddingUnavailable`, not a relabelled generation failure), `CompressionFailed` (the local compressor is total — Filter/Extract/Graph only transform and **degrade gracefully to uncompressed** on any downstream problem; the variant is reserved for a future non-degradable compressor leg), and `SubTaskDagFailed` (see SUB-TASK-DAG-VALIDATED-ONLY). |
| **PBT-GATE-MANDATORY** | 2026-09-09 | The property-based-testing (PBT) gate is **mandatory** for every code-bearing unit. Per unit three artifacts are required: (1) a **typed §5.x Property register** authored by the spec_writer (`P-IM`/`P-SM`/`P-TP` rows, ≤ 8 rows, invariants ONLY — never §6 `FS-n` fail-state or §7 `F-` gap rows, and each row TRUE of the current green implementation); (2) an **executed property layer** (deterministic pinned seed, run under `cargo test`, ≤ 100 generated cases/row, ≤ 400 total, stop-after-5, each row recorded HELD/BROKEN + strategy-id); (3) a **read-only PBT audit** (over-strength reasoning per row, generator-coverage audit, prose counterexamples, negative-generator requests — reviewers never run generators). A code-bearing DONE row without register + property layer + audit is a review finding. **Retrofitted 2026-09-09 to §4.1–§4.5:** 5 registers in `docs/specs/4-*-property-register.md`, 5 executed property layers (`tests/props_{store,graph,facts,consistency,retrieval}.rs`), 5 read-only audits. The gate surfaced and fixed a **genuine host defect**: `rrf_fuse`'s f64 RRF accumulation was non-associative, so mathematically-exact-tied keys landed 1 ULP apart by outer-list order, defeating the id-ascending tie-break and breaking the §4.5.3 exact-merge determinism (register P-IM-2) — fixed by summing each key's contributions in canonical (ascending-sorted) order + a regression test. Zero-row exemption requires an explicit recorded, justified exception (doc/config-only units). |
| **F2-WIRE-CONTRACT-A1** | 2026-09-09 | The F2 engine-transport unit (§7.2) is scoped as a **mechanism-agnostic wire contract + encoding layer**, not a hosted HTTP/native-IPC server (proposal-review verdict in `docs/specs/7-2-f2-review.md`, PASS per Architecture A1). Wire surface = the §4.6.1 retrieval trio (`ragQuery`/`ragStream`/`getEngineStatus`) + health, **not** the full 34-method `RagStore` CRUD. `StoreError` and `RagChunk` are encoded onto the existing FS taxonomy (`wire_code` map; `RagResult` body already `Serialize`) in a new `src/wire/` module — zero new runtime deps. `EngineError`/`TraceUnavailable` are realized via a **decode-then-validate** step (malformed-body → `EngineError`; missing `trace` → `TraceUnavailable`), since the engine has no in-repo producer for them. SSE is **single-event** (`type: result|done|error`), honest to the single-shot `rag_stream`. A versioned `schemaVersion`/`idFormat` **envelope seam** carries the deferred UUID-v4 id decision (ids cross the wire as opaque strings, so switching the id scheme never changes the wire shape); RFC-4122 adoption and the upstream-spec reconciliation are deferred to the suite/HANDOFF. PBT gate applies (wire property register + `props_wire.rs` + audit). The HTTP status map (incl. `ConflictError`=409 per FS-4), SSE client, bind/auth/TLS, boot→READY lifecycle, full CRUD routing, and D2-absent shell behavior are recorded in `docs/specs/engine-wire-contract.md` and owned by a later shell-integration unit. **LANDED / REALIZED (2026-09-09):** this scope shipped in `src/wire/` (`error`, `codecs`, `decode`, `sse`, `status`, `envelope`, `mod`) re-exported from `src/lib.rs`, with `tests/wire_conformance.rs` (57) + `tests/props_wire.rs` (8) going green — **360 total tests** — clippy/fmt clean. The 21-variant `wire_code` table, decode-then-validate (`EngineError`/`TraceUnavailable`), single-event SSE, and the versioned envelope match this row + `engine-wire-contract.md` §5–§10 exactly; ids cross the wire as opaque strings and RFC-4122 adoption + the shell-integration unit remain deferred (see `docs/next-steps.md`). |
| **RESOLVE-ENTITIES-AUTHORITATIVE-OVERWRITE** | 2026-09-09 | `resolveEntities` is an **authoritative overwrite**: each call re-stabilizes the durable alias→canonical map to an **acyclic, flat** alias graph — the chosen canonical is never itself an alias (its prior entry is dropped) and every alias in the requested set points **directly** to the canonical, with any prior alias that pointed to a now-re-aliased node re-pointed via path-compression. A later manual call supersedes an earlier one (manual-vs-manual last-write-wins); the §4.2.9.2/§4.2.8.4 "never overwritten by an **automatic** computation" invariant is preserved (only manual calls touch the map). Same-call idempotence (P-TP-2) holds; overlap convergence is now **guaranteed** — no residual alias, no cycle. | §4.2.9.1/§4.2.9.2/§4.2.8.4; P-TP-2 |
| **ID-SCHEME-RECONCILE-MONOTONIC** | 2026-09-09 | Engine-assigned `documentId`/`wikiId` **stay the monotonic counter-prefix scheme** (`doc-{N}`/`wiki-{N}` via an `AtomicU64`), which satisfies the spec's actual invariant (stable, globally-unique, never-reused, distinct-on-recreate — pinned by PBT register P-IM-1/P-TP-3). The spec §4.1.1/§4.1.2 "UUID v4" wording is a **means to an end** (uniqueness/stability), not a format mandate; the monotonic scheme is a valid realization. RFC-4122 adoption is **NOT** taken up: it would add a `uuid` dep, change the wire `id_format` value to `"uuid-v4"` (a wire-contract change — the decoder currently rejects it as `UnknownIdFormat`, pinned by 3 tests), and re-derive the wire/store registers, for no functional gain. The landed F2 wire contract (`F2-WIRE-CONTRACT-A1`) already chose opaque strings + deferral; the `id_format` seam (`"opaque-string-v1"`) is the documented extensibility point for a future scheme change. **Recorded as a suite/HANDOFF reconcile request** to amend §4.1.1/§4.1.2 to "stable, globally-unique, never-reused". Doc/HANDOFF-only — no code-bearing unpark. |
| **F6-EVAL-RE-SCOPED** | 2026-09-09 | F6 (RAG evaluation harness, §7.8) is re-scoped to a **deterministic, ground-truth-labeled contextual precision/recall module** — a dev/QA gate, NOT a runtime contract element (proposal-review verdict in `docs/specs/6-f6-eval-review.md`, PASS). The as-stated F6 (audit-log-fed, faithfulness/relevancy, DeepEval-in-Rust) is **invalid**: the §4.3.4 audit log records only counts (not retrieved results) and is non-durable; the engine produces no generated answer (faithfulness/answer-relevancy need a generated answer = the shell/LLM's job); DeepEval's contextual precision/recall are LLM-as-judge, not deterministic. The re-scoped deliverable: `src/retrieval/eval.rs` (pure deterministic `contextual_precision`/`contextual_recall`/`ndcg_at_k`/`mrr_at_k` over `RagResult` + a labeled relevant-node set) + a `gnosis-eval` `[[bin]]` (reads a labeled eval corpus, seeds a `Store`, runs `rag_query`, reports metrics) + `tests/eval_harness.rs` + `tests/props_eval.rs` + a labeled-corpus fixture. **Zero runtime change** (no `QueryAuditEntry`/`rag_query`/`rag_stream`/`get_query_audit_log` change; zero new runtime deps); the harness re-runs `rag_query` against a READY engine (live data acquisition, deterministic metric math offline). Faithfulness/answer-relevancy + the DeepEval Python harness + answer generation + user-facing retrieval-quality surface are **suite-side, not Gnosis**. PBT gate applies (register pins boundedness/determinism/empty-set handling/monotonicity/perfect-retrieval/no-relevant bounds). Corrected unpark criterion: named artifact + labeled eval set + metric scope pinned + zero-runtime guardrail. **LANDED / REALIZED (2026-09-09):** the re-scoped F6 shipped in `src/retrieval/eval.rs` + `src/bin/gnosis_eval.rs` (re-exported from `src/lib.rs`), with `tests/eval_harness.rs` (11) + `tests/props_eval.rs` (8) going green — **387 total tests** — clippy/fmt clean. The four pure metric fns, the `gnosis-eval` `[[bin]]` (deterministic fake + real Ollama `LiveProvider`), the labeled-corpus fixture, and the 8/8 HELD PBT rows match this row + `docs/specs/6-f6-eval-spec.md` exactly. |
| **F4-COMMUNITY-RETRIEVAL** | 2026-09-09 | F4 (community summaries, §7.5) is re-scoped to a **retrieval surface**, not a derivation surface (proposal-review verdict in `docs/specs/f4-community-summaries-review.md`, PASS). The **derivation** surface is **invalid**: `declareCommunity` REQUIRES a non-empty manual summary, so every community already has an authoritative manual summary (nothing to fill in — vacuous), and a deterministic concatenation of member node values duplicates `rag_query` graph mode (redundant). The re-scoped deliverable: **`getCommunityContext(communityId) → CommunityContext`** — a new read-only `RagStore` trait method returning `{community_id, wiki_id, summary, members, state}` (the authoritative manual summary + member set + current `CommunityState`), a pre-joined read of `get_community` + `community_state`. Fail-state: **`CommunityNotFound` only** (a `communityId`-keyed accessor derives the wiki, so `WikiNotFound` cannot fire). **Zero runtime change** (no new deps; no provider; no derivation; no mutation; side-effect-free). **§4.2.8.4** (manual summary never overwritten) preserved by construction (read-only); **§4.4.3** staleness/publish gate unchanged; `re_derive_community` keeps its "clear STALE" semantics; **FS-24 `EnrichmentBudgetExceeded` stays RESERVED**. The wire codec for the community surface is deferred to the shell-integration unit (does NOT extend the F2 wire contract). PBT gate applies. **Suite-side / parked:** LLM-generated community summaries (needs a text-generation provider — a NEW seam; `EmbeddingProvider` is embed-only), hierarchical community detection, global/theme retrieval (the §7.5 benefit), the full GraphRAG pipeline, and any `derived_summary` field / `deriveCommunitySummary` API. **LANDED / REALIZED (2026-09-09):** the re-scoped F4 shipped in `src/store/mod.rs` (`CommunityContext` + `get_community_context`, re-exported from `src/lib.rs`), with `tests/community_context_integration.rs` (15) + `tests/props_community_context.rs` (9) going green — **411 total tests** — clippy/fmt clean. The accessor is a pre-joined read of `get_community` + `community_state`; fail-state `CommunityNotFound` only; 6/6 PBT rows HELD. |
| **P1A-DOCUMENT-CRUD-WIRE-LANDED** | 2026-09-10 | The **11 §4.1 document-CRUD wire shapes** are **frozen** (the MVP wire unit, §7.2 P1a): the 11 `CrudMethod` request/response shapes (`createDocument`/`getDocument`/`updateDocument`/`deleteDocument`/`publishDocument`/`unpublishDocument`/`archiveDocument`/`listDocuments`/`createWiki`/`getWiki`/`listWikis`), each wrapped in the F2 `Envelope` (`schemaVersion:1`, `idFormat:"opaque-string-v1"`); the camelCase `"method"` discriminator + per-method `args`/`result`/`error` bodies; the **endpoint paths** (the 11 pinned `ENGINE_ENDPOINTS` rows, bijective with the 11 methods); the **RBAC `caller` shape** (a `caller` string field on the 7 mutating request args, absent on the 4 read-only); the **NEW-2 request-decode outcome** (malformed/unknown request → transport-level **400/422**, NOT 502, NOT a `StoreError`, no §11 row); and the **§5.x Property register** (8 rows, all HELD). The graph/fact/consistency/RAG-companion wire shapes (P1b–P1e), the server (P2), and the shell client (A1–A2) are deferred follow-ons. **LANDED / REALIZED (2026-09-10):** shipped in `src/wire/crud.rs` + `DecodeError::UnknownMethod` in `src/wire/decode.rs` + the re-export surface in `src/lib.rs`, with `tests/crud_wire_conformance.rs` (35) + `tests/props_crud_wire.rs` (8) + `tests/blind_p1a_crud_wire_greens.rs` (21) going green — **454 total tests** (411 baseline + 43; the 21 blind-greens are a separate verification layer) — clippy/fmt clean. |
| **P2-GNOSIS-SERVER-LANDED** | 2026-09-10 | The **`gnosis-server` binary crate** (§7.2 P2) is **LANDED** — the thin live HTTP/SSE transport host. It binds **loopback-only** to `127.0.0.1:<port>` (a non-loopback bind is a fail-state; the shell owns bind/auth/TLS policy), hosts the **14 endpoints** (the 3 retrieval-trio + health `POST /rag/query`, `GET /rag/stream` SSE, `GET /engine/status` PLUS the 11 document-CRUD REST paths consumed verbatim from P1a `ENGINE_ENDPOINTS`), runs the **READY boot lifecycle** (`Arc<Store>` → `DerivedIndexes` → embedding provider → `READY`/`DEGRADED`/`UNAVAILABLE`; the server does NOT fabricate READY — without a wired provider the engine stays DEGRADED/UNAVAILABLE), renders the **§11 HTTP-status map server-side** (21 rows, `server_status`) **plus the NEW-2 request-decode outcome** (malformed/unknown request → **400/422, never 502**, `request_decode_status` — a transport-level status, NOT a `StoreError`, no §11 row), and **threads the RBAC `caller` through the request-decode layer only** (the decode layer validates the `caller`'s presence on the 7 mutating methods; engine-side RBAC enforcement is a PACKAGE item — the engine methods are called without a `caller` param). The pure mapping fns live in `src/server.rs` (`server_status`/`request_decode_status`/`route_bijection` — the 14-row routing bijection), re-exported from `src/lib.rs`; the bin is `src/bin/gnosis_server.rs` with the axum framework scoped to the bin (the engine lib stays at zero new runtime deps). **§5.x Property register** (7 rows: P-IM-1..3, P-SM-1..3, P-TP-1), all **HELD**. **LANDED / REALIZED (2026-09-10):** `tests/gnosis_server_conformance.rs` (25) + `tests/props_gnosis_server.rs` (7) + `tests/gnosis_server_e2e.rs` (16) + `tests/blind_p2_gnosis_server_greens.rs` (15) going green — **523 total tests** (454 baseline + 48; the 15 blind-greens are a separate verification layer) — clippy/fmt clean. Live-scenario: **15/15 server-endpoint scenarios PASS live**; the **20 MCP/UI parity scenarios M1–M20 are PARKED on A1** (the Astrographer CRUD routing client). |
| **P1A-P2-HYGIENE-TRIO-FIX** | 2026-09-10 | The P1a/P2 landing's "clippy 0 warnings · fmt clean" trio claim was **stale**: `cargo fmt --check` exited 1 (48 diff sites across the 9 P1a/P2 files) and `cargo clippy --all-targets` emitted ~50 warnings (41× `clone` on `Copy` `CrudMethod`, 4× `clippy::doc_lazy_continuation`, 3 dead reference-enumerator fns, 2 `clippy::zombie_processes`). Fixed as **pure formatting + lint hygiene (no behavioral change)**: `cargo fmt`; `cargo clippy --fix --all-targets`; a doc-para blank-line fix; justified `#[allow(dead_code)]` on the three spec-citation **reference enumerators** (`crud_reachable_errors`/`retrieval_trio_errors`/`decode_error_variants` — documented variant-set enumerators, retained for spec-citation value); justified `#[allow(clippy::zombie_processes)]` on the two `spawn_server` helpers (the `ServerGuard`'s Drop already `kill()`+`wait()`es the child, so no zombie is left). Re-verified trio: **`fmt --check` exit 0 · `clippy --all-targets` 0 warnings · `build` clean · `cargo test` 538 pass / 0 fail**. Governing rule pinned: a trio claim in a DONE row / handover state must be **re-verified against the actual tree**, not carried forward — regression risk when code is added with `cargo fmt`/`clippy` not run on the landed diff. | docs/decisions.md rows P1A-DOCUMENT-CRUD-WIRE-LANDED + P2-GNOSIS-SERVER-LANDED |
| **GNOSIS-RBAC-EDIT-ENFORCEMENT** | 2026-09-10 (**AMENDED to align with C5 — RBAC → SHELL**) | The **RBAC edit-authority enforcement lives in the SHELL, not the engine.** The engine's `RagStore` mutating methods take **no `caller` param** and the engine holds **no authority mapping** (the only "who may edit" mapping is the shell's boot-time `AuthorityStore`, `callerId → credential`, from operator settings). The shell's A2 caller-side deny (`handleGnosisTool` `resolveCallerCredential` → `Error('<tool>: caller has no edit authority')`, fail-closed, before any proxy call) **is** the enforcement. The P2 `gnosis-server` threads the `caller` through the **request-decode layer only** (validates presence on the 7 mutating methods → 400 if missing; does NOT pass it to the engine). This **amends** the earlier framing that named the engine as the enforcer: the C5 boundary classification (`docs/research/astrographer-engine-shell-boundary.md` line 87 — "RBAC to selective versions → SHELL. The authorization gate at the access boundary → SHELL (a policy/interface concern)") + the pure-backend framing (AGENTS.md — "Gnosis is a pure backend") place the authorization gate at the shell. **No engine-side RBAC enforcement** (no new `StoreError` variant, no §11 row, no `wire_code()` entry, no `P-IM-3`/codec change, no trait change). Residual risk (accepted): the engine's mutating methods are callable by any process that can reach the loopback port directly; acceptable under the loopback-only + local-shell-only trust model. **Trigger condition for revisiting:** a future non-shell direct client → mitigation is a caller-aware decorator at the access boundary (Option B), NOT a trait change. Proposal-review record: `docs/specs/rbac-caller-review.md`. | C5 boundary; P2 §8; A2 `AuthorityStore` |
| **AUTHORSHIP-SOURCE-PROPERTY** | 2026-09-09 | The enrichment surfaces (`declareCommunity`/`updateCommunitySummary`/`resolveEntities`/`mergeFacts`) gain a **persisted authorship provenance** `{source, authority}` on each record (`Community.authorship`, a new `entity_authorship` map keyed by canonical, `Fact.authorship`), set from a per-call `authorship` parameter (the caller presents it, like `requester`; the engine persists the authoritative copy). `authority` is a **total-ordered enum** `System < Agent < LlmHost < HumanJunior < HumanSenior` — the lock's comparison key; `source` is the attribution dimension (`Human|Agent|LlmHost|System`). A future **lock** (opt-in, default-OFF) enforces `authority(new) >= authority(prior)` **in the engine** (the single place holding both the prior record's authority and the new call's), rejecting a lower-authority override with `InsufficientAuthority` (**RESERVED** per RESERVED-ERRVARIANTS-DISCIPLINE until the lock lands). This **refines** §4.2.8.4 (an agent/LLM-host is still a manual caller, not an automatic computation) and **strengthens** it (a human's declaration can't be overridden by a lower-authority caller when the lock is on). **Additive** in contract terms (optional field/parameter, default-OFF) but **code-bearing** (changes `Community`/`Fact`/options/trait + the 454 pinned tests) — **doc/design-only now**; the code-bearing TDD unit (with PBT register + property layer + audit) lands when the F4-LLM integration or a concrete lock use case unparks. | §4.2.8/§4.2.9; §4.2.8.4; RESOLVE-ENTITIES-AUTHORITATIVE-OVERWRITE; F4-LLM |
| **GNOSIS-CHANGE-CURSOR** | 2026-09-16 | The engine owns **no consumer-visible store-wide, point-in-time revision**. The inbound `GR-4` request for `GET /snapshot?revision=…` (a monotonic revision bumped per committed write) and its `stale_revision` error are **REFUSED**: under `SHARDED-RWLOCK-STORE` there is no global store lock, so no point-in-time read across shards exists to back the claim; the only store-wide counter is the derived-index `epoch`, which legitimately advances on propagation/state-annotation journal mutations and therefore yields **phantom** changes to a consumer; and `stale_revision` would be a variant whose generating step cannot run — barred by `RESERVED-ERRVARIANTS-DISCIPLINE`, while §11's 21-row map may not grow. Instead the engine exposes exactly **one wire-visible change cursor**: an **opaque string equal to the committed journal `seq`**, advancing by **exactly 1 per committed journal entry**, **process-lifetime monotonic** (it may repeat across restarts until a durable store exists — see `ENGINE-DURABLE-CORPUS-DIRECTION`), with its **own accessor and documentation distinct from `epoch`**. Its documented scope is **cache invalidation, resync and de-dup ONLY** — explicitly **not** point-in-time, and **not** valid for cache validation, optimistic concurrency or state reconstruction. Per-document `revision` remains the concurrency token. It **refines** `IMMUTABLE-DERIVED-SNAPSHOT` + `WRITER-ACTOR-JOURNAL` (adding an explicit commit-cursor accessor) and **supersedes nothing**; it preserves §4.2.7.2 + `GRAPH-OWNS-RELATION-AND-MERGE` because the consumer never owns graph truth — its projection is a **cache**. If true as-of-N reconstruction is ever required it is a **separate journal-retention unit** (boundary **C4**), belonging inside the durability design unit — **not** a query route. **LANDED AS CONTRACT by U1 (2026-09-16):** the cursor contract is pinned in `docs/specs/engine-wire-contract.md` §4.6 (cursor) + §4.7 (`GET /changes`) and `docs/specs/p2-gnosis-server.md` §5.6 (U1's spec-gate review returned EMPTY ⇒ the spec gate is OPEN); the code-bearing unit (**U4**) is **NOT yet authorized**. | Gate-1 review record `docs/specs/gnosis-gr-inbound-review.md` (ruling 1 + **Appendix A**), accepted by the user's go-ahead instruction 2026-09-16. Code-bearing unit: **U4 — NOT yet authorized**. |
| **SUBSYSTEM-FLAG-CAPABILITY-SEMANTICS** | 2026-09-16 | An `EngineSubsystems` flag means **"this subsystem's full query-time capability is wired and functional for the current store"** — **not** "a provider exists". Under that definition the six flags hard-coded `true` at construction (`src/store/mod.rs:1718-1725` — the literal, under its U3 comment block `:1714-1717`; the post-U3 citation this row carried, `:1714-1725`, is stale, and the pre-U3 citation was `:1710-1717` — the literal is **dead value after U3**) were a **defect** (`reranker: true` although no reranker implementation exists anywhere in `src/`; `vector: true` although the boot path leaves `vectors: None`), recorded in `docs/defects.md`; `lexical: true` **is** honest under this semantics (live shard-scan BM25). Fix scheduled as **U3** (flag derivation from real state; `vector` stays `false` until **U5**'s boot index build), with the **F2 §9 amendment + the V-8 golden update landing in the same unit as the flag change**. **No new status endpoint**: `/engine/status` stays the single status surface, always-200. **LANDED AS CONTRACT by U1 (2026-09-16):** the flag-capability semantics are pinned in `docs/specs/engine-wire-contract.md` §9.1 + `docs/specs/p2-gnosis-server.md` §5.8 (V-8 golden restated in the same text), so U3 has contract authority for the flag derivation; U1's spec-gate review returned EMPTY (the spec gate is OPEN) and **U3/U5 remain NOT yet authorized**. | Gate-1 review record `docs/specs/gnosis-gr-inbound-review.md` (ruling 4), accepted by the user's go-ahead instruction 2026-09-16. **SUPERSEDED (2026-09-16) as to the unit's authorization: U3 IS AUTHORIZED — its typed rows are AUTHORED in `docs/specs/p2-gnosis-server.md` §9.5.2 (`P-IM-7`/`P-IM-8`/`P-IM-9`/`P-SM-5`/`P-SM-6`, 5 rows) with the execution plan §9.5.3; U5 remains NOT authorized.** The historical clause read: Unit: **U3 — NOT yet authorized**. **RESOLVED (2026-09-17): U3 LANDED-GREEN** — the flags are derived at read time inside `get_engine_status` (`src/store/mod.rs:4193-4232`), the boot seam is `boot_wiring` (`src/lib.rs:69-110`), the boot writes **no** flag mask (`src/bin/gnosis_server.rs:378-408`), and the V-8.1/V-8.2 golden literals were amended in the same unit (`tests/wire_conformance.rs:1061-1112`); the defect row in `docs/defects.md` is marked **FIXED (U3)**, all five typed rows HELD (127 executed ≤ 400), the blind set is 13/13 and the live battery 9/9 (incl. `R-L2`). **U5 remains the only unit that flips `vector` to `true`** — the boot index build, still NOT authorized. |

Design details in `docs/research/gnosis-data-structures-concurrency-plan.md`.

The Gnosis spec pins new decision rows when the relevant unit lands, e.g.
RESULT-LEVEL-PROVENANCE, QUERY-AUDIT-LOG, GRAPH-MODE-WALK,
REFERENCE-GRAPH-ADDITIVE-FIELDS, FANOUT-INTERLEAVE-MERGE.

## SUPERSEDED

_(none yet.)_
