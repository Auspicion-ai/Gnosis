# Gnosis — Pending / Parked / Speculative

Maintained by the document-archival loop. Three kinds of rows: (a) UPSTREAM —
constraints owned by the Auspicion Suite top-level architecture repo that Gnosis
must respect; (b) DEFERRED — lower-value gaps parked until a use case surfaces;
(c) SPECULATIVE — future features with their revisit conditions.

## UPSTREAM (imported constraints)

| Constraint | Source | What Gnosis does |
| --- | --- | --- |
| Honor the canonical contract `docs/specs/gnosis.md` | `../Auspicion Suite/docs/specs/gnosis.md` | Every behavior is derived from the contract; a change goes through the suite proposal-review gate. |
| Gnosis is a pure backend — no MCP/GUI surface | spec §4.6.2 | Gnosis exposes its API to the shell; D4 parity applies at the shell. |
| Incanter is archived as a prototype | decision GNOSIS-ENGINE | Gnosis supersedes Incanter; do not reuse Incanter as the production engine. |

## DEFERRED / PARKED (from the spec §7 gaps + the parked retrieval surfaces)

| Item | Disposition |
| --- | --- |
| **F2 — engine transport/API reconciliation** | **SUPERSEDED IN PART (2026-09-09) — F2 LANDED.** The mechanism-agnostic **wire contract** (the §7.2 F2 engine seam) is now implemented + green (`docs/specs/engine-wire-contract.md`, `src/wire/`, `tests/wire_conformance.rs` + `tests/props_wire.rs`; see `docs/next-steps.md` F2 DONE row). What remains deferred is the **transport mechanism** — the exact HTTP-over-native-IPC server + shell-side SSE client + bind/auth/TLS + full `RagStore` CRUD routing + boot→READY lifecycle + HTTP-status rendering + D2-absent shell behavior — which is owned by a **later Astrographer-shell integration unit** (recorded in `docs/specs/7-2-f2-review.md` §"Deferred / OUT of F2", `docs/specs/engine-wire-contract.md` §2/§14, decision `F2-WIRE-CONTRACT-A1`). Design at the shell seam unit. |
| **P1b–P1e + P2 + A1–A2 — the remaining CRUD wire + server + shell-client** | **P1a + P2 + A1 + A2 LANDED (2026-09-10, decisions P1A-DOCUMENT-CRUD-WIRE-LANDED + P2-GNOSIS-SERVER-LANDED).** The **document-CRUD wire shapes** (the 11 §4.1 request/response shapes, the codecs, the RBAC `caller` shape, the endpoint paths, the NEW-2 request-decode outcome) are frozen + green (`docs/specs/p1a-document-crud-wire.md`, `src/wire/crud.rs`, `tests/crud_wire_conformance.rs` + `tests/props_crud_wire.rs`; see the P1a DONE row in `docs/next-steps.md`), the **`gnosis-server` binary crate** (the loopback server host that serves the CRUD + retrieval-trio endpoints, the §11 status rendering + NEW-2 request-decode outcome, the READY lifecycle, the RBAC `caller` threading) is implemented + green (`docs/specs/p2-gnosis-server.md`, `src/bin/gnosis_server.rs` + `src/server.rs`, `tests/gnosis_server_conformance.rs` + `tests/props_gnosis_server.rs` + `tests/gnosis_server_e2e.rs`; see the P2 DONE row in `docs/next-steps.md`), the **Astrographer-side CRUD routing client (A1)** (`createEngineCrudRagStore`, the 11 §4.1 document-CRUD wire client over the frozen P1a wire) is implemented + green, and the **Astrographer-side document-CRUD D4 wiring (A2)** (the 11 `gnosis.document.*`/`gnosis.wiki.*` MCP tools + the `gnosis-edit` group + the extended `handleGnosisTool` + the `AuthorityStore` + the `IdempotencyRegistry` + the GUI document-editor/wiki screens over the A1 proxy — the final MVP unit) is implemented + green (see the A1 + A2 DONE rows in the Astrographer `docs/next-steps.md` + the ENGINE-CRUD-WIRE-CLIENT / ENCODE+DECODE / P4-IDEMPOTENCY-RETRY + GNOSIS-CRUD-EDIT-GROUP / GNOSIS-DOCUMENT-TOOLS / GNOSIS-RBAC-CALLER-STORE / GNOSIS-409-UX / GNOSIS-IDEMPOTENCY-DEDUP decision rows in the Astrographer `docs/decisions.md`). **THE DOCUMENT-CRUD MVP IS COMPLETE (P1a + P2 + A1 + A2).** What remains deferred: **P1b–P1e** (the graph/fact/consistency/RAG-companion wire shapes — follow-on wire units, NOT part of P1a). The P1a/P2/A1/A2 live-scenario batteries are parked on a running `gnosis-server` + a running app (`docs/specs/p1a-document-crud-wire-live-pending-battery.md`, `docs/specs/p2-gnosis-server-live-pending-battery.md`, `docs/specs/unit-a1-crud-routing-proxy-live-pending-battery.md`, `docs/specs/unit-a2-document-crud-wiring-live-pending-battery.md`). |
| **F3 — Adaptive RAG routing to Zodiac** | **PARKED** (spec §7.4): a query-complexity router; land after the MUST/SHOULD retrieval foundation. |
| **F4 — Community summaries** | **LANDED (2026-09-09, decision F4-COMMUNITY-RETRIEVAL).** The re-scoped F4 — a **retrieval surface**, not a derivation surface — is implemented + green: `getCommunityContext(communityId) → CommunityContext` (a new read-only `RagStore` accessor returning the authoritative manual summary + member set + current `CommunityState`, a pre-joined read of `get_community` + `community_state`) in `src/store/mod.rs` + `tests/community_context_integration.rs` + `tests/props_community_context.rs`. Fail-state `CommunityNotFound` only. **Zero runtime change** (no new deps, no provider, no derivation, side-effect-free); §4.2.8.4 manual-override + §4.4.3 staleness preserved; FS-24 stays RESERVED. **411 tests green**, clippy/fmt clean. **Suite-side / parked:** LLM-generated summaries (needs a text-generation provider — a NEW seam), hierarchical detection, global/theme retrieval (the §7.5 benefit), the full GraphRAG pipeline. See `docs/specs/f4-community-summaries-review.md` + the F4 DONE row in `docs/next-steps.md`. |
| **F5 — LLM-generated dynamic graph query** | **PARKED** (spec §7.6): gated on a graph-query substrate the suite lacks. |
| **F6 — RAG evaluation harness** | **LANDED (2026-09-09, decision F6-EVAL-RE-SCOPED).** The re-scoped F6 — a **deterministic, ground-truth-labeled contextual precision/recall module** (a dev/QA gate, NOT a runtime contract element) — is implemented + green: `src/retrieval/eval.rs` (`contextual_precision`/`contextual_recall`/`ndcg_at_k`/`mrr_at_k` over `RagResult` + a labeled relevant-node set) + the `gnosis-eval` `[[bin]]` + `tests/eval_harness.rs` + `tests/props_eval.rs` + the labeled-corpus fixture. **Zero runtime change** (no §4.3.4/§4.5/§4.6 surface change; zero new runtime deps); the harness re-runs `rag_query` against a READY engine. **387 tests green at F6 landing** (current repo total **411** after F4), clippy/fmt clean. Faithfulness/answer-relevancy + the DeepEval Python harness + answer generation + user-facing retrieval-quality surface remain **suite-side, not Gnosis**. See `docs/specs/6-f6-eval-review.md` + the F6 DONE row in `docs/next-steps.md`. |

## ENGINE-INTERNAL DEFERRED (surfaced by the §4.3 adversarial gate; not spec gaps)

| Item | Disposition |
| --- | --- |
| **Fact-store sharding** | `fact_store` is a single global `RwLock` (a de-facto store-wide write serialization point for fact-heavy loads). **DEFERRED (re-scoped 2026-09-09 after §4.5):** the per-wiki nested map already gives lookup locality and the commit paths are serialized by design (grounding under the integrity lock then commit under `fact_store.write()`), and no §4.5 load path justifies sharding yet. **Refreshed revisit condition:** only a **measured** signal — a throughput/contention benchmark or a fact-heavy multi-writer workload proving the single outer `fact_store` write lock is a bottleneck. The bounded unit, if revisited, is: shard `fact_store` by wiki-hash across N `RwLock`s (mirroring SHARDED-RWLOCK-STORE), preserving the exact commit critical section. |
| **Fact `node_id` coherence** | §4.3.1 says `documentId`/`nodeId` is the fact's location in the store, but the stored `node_id` is a fabricated `fact-{fact_key}` handle, not a live graph `fact` node. **CLOSED as a spec-vs-impl wording tension / spec-reconcile (2026-09-09):** §4.5.4 makes `factKey` the canonical retrieval identity ("similarity ≠ relevance", exact-match), so `factKey` — not `(documentId, nodeId)` — is the fact's true location identity. The engine will **not** auto-materialize a graph `fact` node on commit (that would change `create_fact`/`update_document` authoring semantics and contradict the frozen `Node`/`Fact` model + the manual-override convention §4.3.2a.3/§4.2.8; pinned tests lock in the `fact-<key>` handle). Contract §4.3.1 should reconcile to: a fact's canonical identity is `factKey`; `documentId`/`nodeId` are a derived conventional handle (`nodeId` = `fact-<key>`, corresponding to a live graph `fact` node only when the caller authored one). Reconcile at the spec seam. See `docs/defects.md` + `docs/HANDOFF.md`. |
| **Engine-side query audit-log recording sink** | **RESOLVED (2026-09-09) — the §4.5 query path delivers it.** `Store` carries `query_audit_log: RwLock<Vec<QueryAuditEntry>>`; `rag_query` appends a real entry on every committed query (filters/mode/result_count/timestamp/requester) and `rag_stream` routes through `rag_query`, so §4.3.4 `getQueryAuditLog` returns real entries. Pinned by `tests/facts_integration.rs::get_query_audit_log_returns_audit_entry_shape` and `tests/rag_query_integration.rs::rag_query_appends_query_audit_entries`. |
| **Cross-field consistency gate** | §4.3.2a.1 lists cross-field consistency as a gate check. **CLOSED as a documented non-goal / spec-reconcile (2026-09-09):** the §4.3 gate enforces schema/grounding/dedup only (`rejected_outcome` is invoked only with `code:"schema"`; no `cross-field`/`CrossField` branch exists in `src/`). Enforcing "value consistent with cited nodes' content" needs an LLM/embedding *judgment* on the commit path, which breaks the spec's deterministic fail-closed gate framing (§4.3.2a) and injects a provider dependency into the manual-override flow (§4.3.2a.3). **Recorded as a deliberate non-goal** (`code:"schema"` only). Reconcile spec §4.3.2a.1 to mark cross-field as an aspirational/offline semantic check, **not** enforced by the runtime §4.3 gate; optional re-scope to a SHOULD "future offline fact-validation pass over the §4.5 embedding leg." |
| **Full sub-task DAG (`subTaskDag`)** | Spec'd **opto-in / SHOULD-HAVE** (§4.5.4, same class as adaptive RAG F3, which is PARKED). Currently parsed + validated only; **decomposes nothing** — a single-node decomposition (the query itself) is the honest stand-in, no fake decomposition is fabricated (decision SUB-TASK-DAG-VALIDATED-ONLY). `SubTaskDagFailed` is a RESERVED variant (throwable once the real decomposition lands); no test requires it to throw. **PARKED (engine-internal deferred):** revisit with complex multi-hop queries; the DAG decomposition + a runner that fans sub-queries out (and merges their results) lands here. |
| **Non-degradable compressor leg (`CompressionFailed`)** | The local compressor is **total** (Filter/Extract/Graph only transform; it degrades gracefully to uncompressed — spec-correct). `CompressionFailed` is a **RESERVED** variant for a future compressor leg that cannot degrade on failure. **PARKED:** only implement a throw-path when a real non-degradable compressor lands; do not fabricate a failure mode. |
| **`resolve_entities` residual alias / alias-cycle behavior** | `resolve_entities` records alias→canonical for the requested set but **never removes a PRIOR alias entry** (`src/store/mod.rs` only `insert`s the durable map), so an overlapping re-resolution with a different canonical can leave a residual alias (e.g. `e1→e2` while `e2→e1`); same-call idempotence holds, full convergence does not. **RESOLVED (2026-09-09) — decision RESOLVE-ENTITIES-AUTHORITATIVE-OVERWRITE.** Each `resolve_entities` call is an **authoritative overwrite**: it re-stabilizes the durable alias→canonical map to an **acyclic, flat** alias graph (the canonical is never an alias; every alias points directly to it; prior aliases pointing to a now-re-aliased node are re-pointed via path-compression). A later manual call supersedes an earlier one (manual-vs-manual last-write-wins); the §4.2.9.2 "never overwritten by an **automatic** computation" invariant is preserved. Same-call idempotence (P-TP-2) holds and overlap convergence is now guaranteed — no residual alias, no cycle. Implemented in `src/store/mod.rs` `resolve_entities`; pinned by the updated P-TP-2 overlap test + new overlap-flip/chain-flatten tests. |

## DOC-REVIEW GAPS (surfaced by the 2026-09-09 core doc-review; not spec gaps)

Real gaps found while reconciling the greens/specs to the crate. Each is a
documented-but-untested behavior that **is** implementable/checkable from the
current crate. Preferred recording over forcing a test in this pass because the
session is docs-only (read-only on `src/`/`tests/`).

| Item | Disposition / revisit condition |
| --- | --- |
| **Engine-assigned `documentId` (and `wikiId`) are not UUID v4** | Spec §4.1.1/§4.1.2 say `documentId`/`wikiId` are "stable, globally-unique … (UUID v4)". The crate assigns `doc-{N}` / `wiki-{N}` (a monotonic counter prefix), **not** an RFC-4122 UUID v4. **RESOLVED (2026-09-09) — reconciled to monotonic (decision ID-SCHEME-RECONCILE-MONOTONIC).** The revisit condition fired (F2 landed; the id format is an on-the-wire contract). The landed wire contract (`engine-wire-contract.md` §4.1) treats ids as **opaque strings** and does not require UUID v4; the monotonic `doc-{N}`/`wiki-{N}` scheme satisfies the spec's actual invariant (stable, globally-unique, never-reused, distinct-on-recreate — pinned by PBT register P-IM-1/P-TP-3). RFC-4122 adoption is **not** taken up (would add a `uuid` dep + change the wire `id_format` to `"uuid-v4"` for no functional gain). The `id_format` seam (`"opaque-string-v1"`) is the documented extensibility point for a future scheme change. **Reconcile request recorded in `docs/HANDOFF.md`** to amend §4.1.1/§4.1.2's "UUID v4" wording to "stable, globally-unique, never-reused". |
| **`createdAt`/`updatedAt` ISO-8601 UTC format not pinned by any test** | Spec §4.1.1/§4.3.1 require ISO-8601 UTC timestamps. The crate's `iso_now()` does emit `YYYY-MM-DDThh:mm:ss.nnnnnnnnnZ` (ISO-8601 UTC), but no test asserts the format (only non-empty). **CLOSED (2026-09-09, doc-review quick-pin test pass)** — `assert_iso8601_utc` added to `tests/store_integration.rs`/`tests/facts_integration.rs`/`tests/graph_integration.rs` + wired into store create (`created_at`/`updated_at`), fact create/update (`updated_at`), triple `created_at`, and `merge_facts` (`updated_at`); pins the emitted `YYYY-MM-DDThh:mm:ss.nnnnnnnnnZ` shape (30-byte, 9 fractional digits + `Z`) against real values. |
| **`author` metadata not round-trip-tested** | Spec §4.1.1 exposes `author` (string). ~~No test asserts it is stored/returned~~. **CLOSED (2026-09-09, by the PBT-gate retrofit):** the §4.1 `props_store.rs` P-TP-2 row (`p_tp_2_metadata_preserve_update` + the P-TP-3 `p_tp_3_delete_recreate` continuation) asserts `author` is set at create, returned on `get_document`, preserved across graph-only/title-only/tags-only updates, and preserved across recreates. Disposition superseded; removed from the open quick-pin set. |
| **`MultiQueryExpansionFailed` (FS-19) fail-state is reachable but untested** | `expand_query_variants` genuinely returns `MultiQueryExpansionFailed` when a multi-query fan-out (`multiQuery:{enabled,n≥2}`) has **no distinct indexable term to add** (e.g. an empty/near-empty store: `term_popularity` yields nothing). Unlike the other engine-internal variants it is **drivable from a typed test** with no seam. **CLOSED (2026-09-09, doc-review quick-pin test pass)** — `tests/rag_query_integration.rs::rag_query_multi_query_empty_wiki_is_expansion_failed` + `rag_stream_multi_query_empty_wiki_emits_error_chunk` drive FS-19 via an empty-wiki seed (`term_popularity` yields no term → `expand_query_variants` returns `MultiQueryExpansionFailed`); reachability confirmed through the public API, no seam. |

## SPECULATIVE

| Item | Disposition / revisit condition |
| --- | --- |
| **F4-LLM — automatic community / manual-enrichment pass via a harnessed LLM** | **SPECULATIVE / FUTURE-INTEGRATION (2026-09-09).** A suite tool with a **harnessed LLM** (Familiar, Astrographer, or Emerald) drives Gnosis's existing **manual-override-authoritative** enrichment surfaces (`declareCommunity`/`updateCommunitySummary`, `resolveEntities`, `mergeFacts`) to run an automatic enrichment pass — the LLM *proposes*, Gnosis's manual-override invariants stay authoritative (the LLM host is a caller, not an engine-internal automatic pass). **Not a Gnosis-repo code unit** (Gnosis has no text-generation provider; `EmbeddingProvider` is embed-only). **Revisit condition:** a suite tool with a harnessed LLM lands a text-generation seam and wants to drive Gnosis's enrichment surfaces; if a wire surface is needed, the shell-integration unit must land first. Contract: `docs/specs/f4-llm-enrichment-integration.md`. |
| **Authorship-source property + future locks** | **SPECULATIVE / DESIGN-RECORDED (2026-09-09, decision AUTHORSHIP-SOURCE-PROPERTY).** A persisted `{source, authority}` provenance on the enrichment records (`Community`/`entity_authorship`/`Fact`), set from a per-call `authorship` parameter, enabling a future **opt-in engine lock** (`authority(new) >= authority(prior)`, `InsufficientAuthority` RESERVED). `authority` is a total-ordered enum `System < Agent < LlmHost < HumanJunior < HumanSenior`; `source` is the attribution dimension. **Doc/design-only now** — additive but code-bearing (changes `Community`/`Fact`/options/trait + the 454 pinned tests). **Revisit condition:** the F4-LLM integration unparks (a suite tool with a harnessed LLM drives the enrichment surfaces) or a concrete lock use case (agents cannot override human input; junior staff cannot override senior-staff changes) surfaces. Design: `docs/specs/authorship-source-review.md`. |

## PARKED per the gate-1 review of the inbound GR-1..GR-9 set (2026-09-16 — **U0 + U1 landed (2026-09-16); U2/U3 AUTHORIZED (2026-09-16 — their typed registers are authored in `docs/specs/p2-gnosis-server.md` §9.5.1/§9.5.2); U2's code LANDED 2026-09-17; **U3's code LANDED-GREEN 2026-09-17, so no U2/U3 code is owed any longer**; **U5 AUTHORIZED (2026-09-22) and LANDED-GREEN (its spec gate EMPTY at round 5; all eight gates run — `docs/next-steps.md` §DONE's U5 row)**; **U4 HELD (it alone needs the consumer-side `SHELL-2`)**; spec gate OPEN**)

Parked by the proposal-review gate on the inbound Astrographer feature-request set
(record: `docs/specs/gnosis-gr-inbound-review.md`). **Nothing here is authorized or
in force** — these rows are parked, each with the **measured un-park trigger** the gate
pinned (and the GR-7 row additionally records the user's roadmap direction from open
question 4). The go-ahead has now covered **U0 (landed) + U1 (the P2/F2 contract amendment,
LANDED 2026-09-16 — its spec-gate reviewer loop returned EMPTY, so the spec gate is OPEN)**,
while **U2/U3 are AUTHORIZED (2026-09-16 — their §9.5 registers are authored; only the code was owed, and both
codes have since landed: U2 realized-green, U3 landed-green 2026-09-17) and
U4/U5 remain HELD** pending the user's next go-ahead (U4 additionally needs the consumer-side `SHELL-2`; U5
follows U3 — it owns the boot vector-index build and the `vector:true` flip). **UPDATE (2026-09-17, proofread
pass): U2's code has since LANDED REALIZED-GREEN** (`cargo test` 582 / 0, U2's property layer 400 cases HELD,
blind set 24/24, the live battery's `R-L1` PASSED live — `docs/specs/p2-gnosis-server.md` §U1), so the
"only the code is owed" clause now applies to **U3 only**; **U4/U5 remain HELD and NOT authorized**. **SUPERSEDED
AGAIN (2026-09-17, the U3 documentation pass): U3's code has LANDED-GREEN** (`cargo test` 604 / 0 serial, its
five §9.5.2 rows HELD with 127 executed ≤ 400, the blind set 13/13, the live battery 9/9 incl. `R-L2`), so **no
U2/U3 code is owed any longer** — **U4 is the only unit still HELD and NOT authorized** (U5 was authorized on
2026-09-22 and has since **LANDED-GREEN** with all eight gates run: `docs/next-steps.md` §DONE's U5 row +
`archive/reviews/2026-09-22-u5-boot-vector-index-doc-review.md`). Do not start any of the rows
below from this table alone. **U5's own OPEN residues are NOT parked rows: they are the six OPEN defect rows
`U5-ADV-1`…`U5-ADV-5` + `P-9`'s fired trigger in `docs/defects.md` (a follow-up "U5 honesty/freshness" unit is
being opened for `U5-ADV-1`), and the PBT audit's `T1`–`T10` negative-generator list is the TestWriter's
undischarged obligation.** **Three U2-pass findings are parked with the transport/U2 work:** `P-5`
(SSE error-frame media type — **OPEN**) in `docs/defects.md` §OPEN, and `P-6` (the wrongly-typed-`n`
rule-vs-code divergence — **FIXED/CLOSED 2026-09-16**: the pinned clause stood and the decoder was implemented
to it) plus its wording-only companion `P-7` (**FIXED/CLOSED docs-side**), both of which stay in
`docs/defects.md` §OPEN as the historical record with their resolutions. **Two U3-pass findings are parked
with the same transport/status work (2026-09-17; neither is a U3 regression):** `P-8` (post-boot provider
loss is invisible to `/engine/status` — **OPEN**, owner = a later transport/status unit, which must either
re-probe on the status read or pin an explicit staleness window) and `P-9` (`encode_result`'s `expect`
panic path for a non-finite `score` — **OPEN**, owner = the F2 wire foundation / the transport unit), both in
`docs/defects.md` §OPEN and cross-filed in `docs/HANDOFF.md` §"U3 documentation pass (2026-09-17)".
**The U3 post-greens documentation review (gate 8, 2026-09-17) verified these statuses against the tree** —
record: `archive/reviews/2026-09-17-u3-status-honesty-doc-review.md`.

| Item | Disposition / measured un-park trigger |
| --- | --- |
| **GR-3 vector-index maintenance on store changes** ("maintain the index as the store changes" — the second half of GR-3, distinct from the boot build) | **PARKED per the gate-1 review (go-ahead pending).** GR-3's status-honesty half is scheduled as **U3** and the boot index build as **U5**; the *maintenance* half needs the **deliberately held-off `WRITER-ACTOR-JOURNAL` writer** (`docs/decisions.md` — the tokio writer-actor is held off until §4.2–§4.4 need the async rebuild/audit feed and the write-path latency is justified). **Trigger:** the writer-actor un-parks on a **measured** rebuild-latency/contention signal **and** U5's boot build is green (**met 2026-09-22 — U5 LANDED-GREEN, gate 8; the writer-actor conjunct alone remains**; note also that U5's landed state makes this row's motivating defect *sharper*, not milder: `U5-ADV-1` in `docs/defects.md` records that a post-boot write leaves the boot index stale while `vector` still reports `true`, so index **maintenance** is now a filed, live-confirmed gap with an owner — the "U5 honesty/freshness" unit — rather than only a parked request). |
| **GR-4 revisioned projection framing** (`/snapshot?revision=` + a `stale_revision` error code, keyed on a consumer-visible store-wide revision) | **RE-FILED by `GRQ-4` (2026-09-21) — the REFUSAL STANDS, and the re-open trigger is THIS row's two conjuncts (a demonstrated stale-read need per-document `revision` cannot serve **and** a possible point-in-time store-wide read); the inbound's weaker "the engine answers the token question" wording does NOT govern — see `docs/specs/gnosis-grq-inbound-review.md` §4/§11.** **PARKED = REFUSED per the gate-1 review (go-ahead pending); the contract half of the refusal is now LANDED by U1 (2026-09-16).** The engine owns **no consumer-visible store-wide revision** — the only counter is a per-process `epoch` that also bumps on propagation/state-annotation mutations (`src/store/mod.rs:2284,2356,3348-3357`); a consumer-held projection keyed on it is a **second source of truth** (§4.2.7.2 + decision `GRAPH-OWNS-RELATION-AND-MERGE`), and `SHARDED-RWLOCK-STORE` forecloses a point-in-time read across shards. The **bulk read** half is re-shaped as **U4** (paginated, wiki-scoped, cursor-tagged). **Trigger to re-open:** a consumer demonstrates a **stale-read need that per-document `revision` cannot serve** *and* a point-in-time store-wide read becomes possible (i.e. `SHARDED-RWLOCK-STORE` is revisited). |
| **GR-6a bulk markdown ingestion — atomicity / cap / progress / cancel** | **RE-FILED by `GRQ-5` + `GRQ-6` (2026-09-21) — PARKED UNCHANGED: the re-filing leaves the trigger UNDISCHARGED (it restates the one-commit-vs-chunked contradiction and resolves neither half), its "…(a)/(b)/(c) trigger set" does NOT exist in this repo, so THIS row's single conjunctive trigger governs, and `{files:[paths]}` stays REJECTED. An over-cap outcome is a transport-level **413-class** outcome with its own register row — never a new §11 row, never a new `StoreError` variant. Record: `docs/specs/gnosis-grq-inbound-review.md` §7(iii)/§11/§15.5 (condition 8).** **PARKED per the gate-1 review (go-ahead pending).** Confirmed absent (no parser, no route, no batch primitive), but the requested contract is **self-contradictory** ("the whole corpus commits as one journal entry" vs "chunked commits or an SSE progress stream"). **Trigger:** an accepted spec resolving **one-commit-vs-chunked** + a **cap** + a **§11 fail-state allocation**. |
| **GR-6b document `path` / path-segments** | **RE-FILED by `GRQ-5`/`GRQ-6` (2026-09-21) — PARKED UNCHANGED (THIS row's trigger governs: a P1a wire-version decision for `path` + an explicit user decision on the filesystem capability, which stays REJECTED); the record-copy shape additionally owes an **id-namespace** entry question against the engine's monotonic `next_id` (`src/store/mod.rs:1556`, `:1697`).** **PARKED per the gate-1 review (go-ahead pending)**; the `{files:[paths]}` variant is **REJECTED** (it would grant the engine a **filesystem-read capability** on a loopback-only server whose accepted residual risk is "any process that can reach the loopback port may call the mutating methods"). The `path` half collides with the **FROZEN** P1a `Document` body (`docs/specs/p1a-document-crud-wire.md:253-256`, `P-IM-2`). **Trigger:** a **P1a wire-version decision** for `path`, plus an **explicit user decision** on the filesystem capability. |
| **GR-7 server-side persistence for the engine store** | **RE-FILED by `GRQ-1` + `GRQ-2` + `GRQ-11` (2026-09-21) — PARKED / DIRECTION-ONLY UNCHANGED: the 3-conjunct trigger below governs (the inbound's "(a) design scheduled/started or (b) refusal" summary omits the O-8 conjunct and the scheduled-consumer-unit conjunct), and the **durable-authority fork** the set never asks is now recorded as an upstream ask in `docs/HANDOFF.md`. The gate makes the design unit **coupled to the consumer's O-8 amendment** and **unauthorizable before the fork answer** — conditions 2/3, `docs/specs/gnosis-grq-inbound-review.md` §7(i)/§13 Q1–Q2/§15.5.** **PARKED per the gate-1 review, THEN RE-STATED by the user's 2026-09-16 gate answer — a durability DESIGN unit is now ON THE ROADMAP (not "parked indefinitely").** The engine store is confirmed in-memory (`let store = Arc::new(Store::new())`, only `--port`), and persistence+revisioning **are** engine-owned work (`docs/specs/gnosis.md:82,190`; boundary A1) — but the request **raises the decisive questions it does not answer** ("what a revision means across a restore", crash-mid-commit), it would re-open `SHARDED-RWLOCK-STORE`/`IMMUTABLE-DERIVED-SNAPSHOT`, and it collides with the consumer's own parked `SINGLE-WRITER-STORE` (O-8) amendment. (The request's "the store trait's persistence seam is a trait with no disk implementation" is a **misstatement**: there is **no persistence abstraction in the crate at all**.) **USER-ANSWERED DIRECTION (2026-09-16, open question 4 → "Yes — schedule a durability design unit"):** a durability **design** unit — scope: **format / atomic commit / crash recovery / store-scope revision semantics** — is **on the roadmap**; it is **NOT authorized to start** (the go-ahead covered **U0 + U1** — U1 **landed 2026-09-16** — and **U2/U3 were subsequently AUTHORIZED (2026-09-16, registers authored; U4/U5 stay HELD)**, so it begins only on a future go-ahead) and it remains **gated on the consumer's `SINGLE-WRITER-STORE`/O-8 amendment being recorded plus a scheduled consumer unit**. Recorded as **`ENGINE-DURABLE-CORPUS-DIRECTION`** — **DIRECTION ONLY / NOT ACTIVE** in `docs/decisions.md` (no ACTIVE decision row: the unit that would pin a durable store is the durability design itself, and it is not authorized). **Trigger (unchanged):** the consumer's **O-8 amendment recorded** + the **durability design** (format, atomic commit, recovery, store-scope revision semantics) + a scheduled consumer unit. |
| **GR-8 enrichment/traversal routes + a text-generation seam** | **PARKED per the gate-1 review (go-ahead pending).** Confirmed absent (the four trait methods `declare_community`/`update_community_summary`/`resolve_entities`/`merge_facts` are implemented but **unrouted**; no text-gen seam exists), but the text-generation seam is **already ruled OUT** (`docs/specs/f4-llm-enrichment-integration.md:139-149`; `docs/pending.md` F4-LLM = SPECULATIVE), and under §4.2.8 / `RESOLVE-ENTITIES-AUTHORITATIVE-OVERWRITE` an LLM host as caller would be **authoritative** and could silently supersede a human declaration while `AUTHORSHIP-SOURCE-PROPERTY` (the only mechanism recording who won) is **unlanded**. **Trigger:** `AUTHORSHIP-SOURCE-PROPERTY` landed **code-bearing** + an accepted text-gen-seam design + a consumer freeze (F4-LLM un-parked). |
| **Route growth beyond U4** (any additional engine route) | **PARKED per the gate-1 review (go-ahead pending).** Ruling 2 turns the pinned 14-row `route_bijection`/`P-IM-3` into a **growth invariant, not a fixed count** — so growth is permitted, but only through an amendment unit. **Trigger:** a named consumer need for the route **plus** an amendment unit that extends `route_bijection()` **and** its conformance/property rows **in the same unit** (the §11 map stays **21 rows** — no new `StoreError` variant, no `stale_revision`/`decode_failed` wire code; a decode failure stays the NEW-2 **transport** outcome 400/422, never 502). |
| **The four CRUD READ routes carry a request body (`GET` with a body) — should they ever stop?** (surfaced 2026-09-22 by the accepted `GRQ-9` docs appendix — `docs/specs/engine-wire-contract.md` §14.2's per-route posture table) | **PARKED (not asked for by the inbound set; recorded so the divergence has an owner).** All 11 P1a CRUD paths are served by the one `crud_handler` (`src/bin/gnosis_server.rs:192-202`) and **no handler reads a `:id` path segment**, so `documentId`/`wikiId` travel in the request envelope — meaning the **four read-only methods** (`getDocument`/`listDocuments`/`getWiki`/`listWikis`) require a GET request body, which **Node/undici rejects while Electron/Chromium permits** (a host-runtime divergence the SHELL adjudicates in-app; **not** a wire defect). If the consumer ever wants param- or path-carried read args, that is a **route/method amendment unit** (`p2` §5.2's amendment rule: `route_bijection()` + the live `router()` + `P-IM-3`'s conformance/property rows + the **5** route-count/CRUD-path assertions **in the same unit**) **plus a P1a wire-version decision**. **Trigger:** a named consumer need for body-less read routes, or a consumer bug report caused by the GET-with-body requirement. |
